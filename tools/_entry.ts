// Re-exports so the inventory generator can import the real data rather than
// parse it. Not used by the site build.
export { projects } from '../src/data/projects';
export { projectWalkthroughs } from '../src/data/project-walkthroughs';
export { landingPages } from '../src/data/landing-pages';
