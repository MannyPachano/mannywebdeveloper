---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Page not found | Manny Pachano" description="That page does not exist. Go back to the homepage or the project list.">
  <section class="notfound-section" aria-labelledby="notfound-title">
    <div class="notfound-content">
      <p class="eyebrow">404</p>
      <h1 id="notfound-title">Page not found</h1>
      <p class="lede">
        The link may be out of date or the address may have a typo. Use the buttons below to get back to the site.
      </p>
      <div class="notfound-actions">
        <a class="button button-small" href="/">Back to home</a>
        <a class="button button-small button-secondary" href="/#project-section">See my work</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .notfound-section {
    min-height: calc(100vh - var(--nav-height));
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 1rem;
    background: linear-gradient(to right, var(--page-wash), var(--page-wash)), url(/images/common-bg.svg);
  }

  .notfound-content {
    text-align: center;
    max-width: 40rem;
  }

  .notfound-content .lede {
    margin: 0 auto;
  }

  .notfound-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem 1.5rem;
    margin-top: 2.5rem;
  }

  .notfound-actions .button {
    margin: 0;
  }
</style>
