# Site copy inventory

Every piece of visible copy on **websitesbymanny.netlify.app**, in the order a
visitor meets it, with the file and field it comes from and what job it does.

Generated by `tools/copy-inventory.mjs`. Regenerate after any copy change.

---

## Before you rewrite anything

**What this site is.** A personal portfolio for Manny Pachano, a front-end
developer and marketing specialist. It is read by prospective clients and
employers, and it doubles as a record of his work history. It is set to
`noindex`, so it is not a search asset. Its only job is to be accurate and
believed.

**The single biggest failure mode.** Previous audits of this copy invented
things: a client engagement that did not exist, a contact section that was
never on the site, and landing page descriptions that quietly dropped the
lines disclosing the work was unsolicited. Do not describe anything you cannot
see in the copy in front of you.

## Hard rules

1. **Invent nothing.** Every figure, client name, date, technology and scale
   claim here is real. Do not add a metric, a result, a tool or an outcome that
   is not already in the text you were given.

2. **Never delete a disclosure.** Three landing pages are unsolicited concept
   redesigns of real organizations, and two are invented businesses. Phrases
   like "built without being asked", "Everything on it is theirs", "an invented
   firm", "none of the figures are real" and "labeled illustrative" are
   load-bearing. Removing them turns a concept piece into a false claim of paid
   work. If a rewrite loses one, the rewrite is wrong.

3. **Do not trade specific for generic.** "A used oil collection company" must
   not become "an environmental services company". "The nine styles he teaches"
   must not become "lesson descriptions". "Suwanee, Georgia" must not become
   "Georgia". The specifics are the value.

4. **Keep the first person.** This is written as Manny, not as a CV. "I built
   it on Astro", not "Built on Astro". No resume verbs: no spearheaded,
   re-architected, engineered, leveraged, utilized.

5. **No unverifiable claims.** No high-converting, lightning-fast,
   industry-leading, world-class, seamless, cutting-edge.

## Voice

- Literal and plain. Say what the thing is and what it does.
- No em dashes and no en dashes. Write ranges as "10 to 20".
- No metaphors, no aphorisms, no sentence that ends on a flourish. If a
  sentence reads like it was written to be quoted, cut it.
- No exclamation marks. No cliches.
- Short sentences. A non-technical reader should follow every line.
- These are captions in a fixed layout. Stay within about ten percent of the
  original length unless you are cutting.

## Two mechanical notes

- Some fields allow inline `<strong>` and `<a>`. Keep the tags unless the
  emphasis genuinely belongs elsewhere.
- Every mention of Clox sits behind one flag (`SHOW_CLOX` in `src/config.ts`)
  so it can be removed site-wide in a single change. Do not add Clox to any
  file that does not already import that flag.

## How to return your edits

For each change, give three things and nothing else:

```
FILE → FIELD PATH   (copy it exactly as printed in this document)
OLD: <the original string, verbatim>
NEW: <the replacement>
```

Do not return rewritten files, and do not renumber or reorder anything.

**Flag rather than fix:** a claim that looks factually wrong, a date that
conflicts with another page, or an inconsistency between two sections. Say so
in a list at the end. Do not guess at a correction.


## 1. Homepage

### Navigation

**File:** `src/components/Header.astro`  
*Site navigation and the logo label. Present on every page.*

Visible text, in page order:

```
MANNY PACHANO
[link.label]
[link.label]
```

Labels and list items defined at the top of the same file:

```ts
// Site header: fixed nav, mobile overlay menu, and the cursor trail (desktop only).
const links = [
  { href: '/', label: 'Home' },
  { href: '/#about-section', label: 'About' },
  { href: '/#project-section', label: 'Projects' },
  { href: '/#landing-pages', label: 'Landing pages' },
  { href: '/#contact-section', label: 'Contact' },
];

const isHome = Astro.url.pathname === '/';
```

---

### Hero

**File:** `src/components/Hero.astro`  
*The first screen of the homepage. Headline, one paragraph, two buttons, and a line naming recent work.*

Visible text, in page order:

```
Hey, I'm Manny Pachano
I'm a front-end developer and marketing specialist. I build websites for software companies and small businesses, and I handle the SEO, email and CRM work behind them. I've worked with US clients since 2019.
See my work Get in touch
Recent work: TimeClick, Universal Environmental Services, nuVector and several small businesses in the US.
```

---

### About

**File:** `src/components/About.astro`  
*Second section of the homepage: who he is, background, skills and tools, and a work history list.*

Visible text, in page order:

```
About me
I work remotely as the front-end developer and marketing specialist at TimeClick, a software company in Utah. I am also the web developer and SEO consultant for Universal Environmental Services, and I take on web design and SEO projects for other US clients. [shown conditionally: I also run my own product, Clox.] Outside of work I rock climb, train Brazilian jiu jitsu and travel.
How I got here
I started building WordPress sites for small businesses while I was in college. Some of the work was paid and some was traded for things I needed at the time. As the projects got bigger, I moved from WordPress themes to writing sites from scratch.
I taught myself JavaScript, PHP and design. From 2022 to 2025 I worked at nuVector, first as a front-end developer on their B2B web apps and later in SEO and marketing. Since 2024 I have been the front-end developer and marketing specialist at TimeClick, where I am responsible for the marketing website, including the design, the code, the CRM and checkout integrations, the SEO content and the email program.
Since 2026 I have also been the web developer and SEO consultant for Universal Environmental Services, a used oil collection and recycling company in Georgia. I rebuilt their homepage and I run their ongoing SEO program.
Get in touch
Skills and tools
[group.name]
[skill]
Experience
[job.org] [job.when]
[job.role] [job.where]
```

Labels and list items defined at the top of the same file:

```ts
import { SHOW_CLOX } from '../config';

// Work history. Keep it factual: employer, role, where, when.
const experience = [
  {
    org: 'TimeClick',
    role: 'Front-End Developer & Marketing Specialist',
    where: 'Remote for a Utah, US software company',
    when: '2024 to present',
  },
  ...(SHOW_CLOX
    ? [
        {
          org: 'Clox',
          role: 'Founder',
          where: 'B2B time-tracking SaaS for small field crews',
          when: '2026 to present',
        },
      ]
    : []),
  {
    org: 'nuVector LLC',
    role: 'Front-End Developer, then SEO & Marketing',
    where: 'US-based B2B software company',
    when: '2022 to 2025',
  },
  {
    org: 'Freelance',
    role: 'Web Design, Development & SEO',
    where: 'Clients across the US',
    when: '2019 to present',
  },
];

// Grouped so a reader can scan what kind of work I do, not just count tools.
const skillGroups = [
  {
    name: 'Development',
    items: ['HTML & CSS', 'JavaScript', 'TypeScript', 'Astro', 'Vue', 'WordPress', 'PHP', 'SQL', 'Git & GitHub', 'Netlify'],
  },
  {
    name: 'Marketing and SEO',
    items: ['Technical & local SEO', 'Google Analytics 4', 'Search Console', 'Google Business Profile', 'Email marketing', 'Conversion copywriting'],
  },
  {
    name: 'Integrations',
    items: ['REST APIs', 'Zoho CRM', 'Stripe & WooCommerce', 'Netlify Forms', 'Payroll and CRM exports'],
  },
  {
    name: 'Design and tooling',
    items: ['Performance optimization', 'Accessibility', 'Responsive design', 'Figma', 'SaaS product design', 'Video for web (ffmpeg, HLS)', 'AI-assisted development (Claude Code)'],
  },
];
```

---

### Projects section intro

**File:** `src/components/Projects.astro`  
*Heading and intro above the project card grid.*

Visible text, in page order:

```
Recent projects
These are projects I have worked on recently. Each case study shows what the project needed, what I built and why I made the decisions I did. If you want to work together, the contact form is at the bottom of the page.
Show  [hiddenCount]  more  [shown conditionally: project | projects]
```

Labels and list items defined at the top of the same file:

```ts
import { projects } from '../data/projects';
import ProjectCard from './ProjectCard.astro';

// On phones, show this many cards first and a "show the rest" button.
const INITIAL_MOBILE_CARDS = 5;
const hiddenCount = Math.max(projects.length - INITIAL_MOBILE_CARDS, 0);
```

---

### Landing pages section intro

**File:** `src/components/LandingPages.astro`  
*Heading and intro above the landing page card grid.*

Visible text, in page order:

```
Landing pages
A landing page is built around one action. These are complete pages I designed and built myself, including the animations and mobile versions. Each one has a section-by-section walkthrough explaining what each part does.
```

Labels and list items defined at the top of the same file:

```ts
import { landingPages } from '../data/landing-pages';
import LandingPageCard from './LandingPageCard.astro';
```

---

### Contact

**File:** `src/components/Contact.astro`  
*Contact heading, intro, and every form label and placeholder.*

Visible text, in page order:

```
Get in touch
Have a website project? Tell me what you need and I'll reply within one business day.
Leave this field empty
First name
Email
What do you need? (select all that apply)
[service.label]
Tell me more
Send message
```

Labels and list items defined at the top of the same file:

```ts
// Contact form handled by Netlify Forms.
// Requirements: static build output (astro.config output: 'static') and
// "Enable form detection" switched on in Netlify > Site configuration > Forms.
const services = [
  { id: 'redesign', label: 'Redesign my current website' },
  { id: 'new-site', label: 'Build a new website' },
  { id: 'seo', label: 'SEO and lead generation' },
  { id: 'other', label: 'Something else' },
];
```

---

### Footer

**File:** `src/components/Footer.astro`  
*Site footer, present on every page.*

Visible text, in page order:

```
Social
Manny Pachano
I'm a front-end developer and marketing specialist. I build websites for software companies and small businesses and handle the SEO, email and CRM work that supports them.
Have a project in mind? Send me a message.
I built this site with Astro 7 and hand-written CSS. It has no trackers, and the homepage is under 1 MB.
Copyright &copy;  [year]  Manny Pachano. All rights reserved.
```

---

## 2. Project cards on the homepage

*Each card shows: title, role, year, and the summary below. Clicking through goes to the case study page of the same name in section 3.*

#### TimeClick
`src/data/projects.ts → timeclick.summary`
*Card summary. Role: Website redesign and marketing. Year: 2024 to present. Tags: WordPress, JavaScript, Zoho CRM, Stripe & WooCommerce, SEO, Email marketing.*

```
I redesigned the marketing website for TimeClick, a time clock software company used by thousands of small businesses, and built the CRM, checkout and email systems behind it. The goal is to get the right visitors to start a free trial.
```

#### Universal Environmental Services
`src/data/projects.ts → ues.summary`
*Card summary. Role: Homepage rebuild and SEO. Year: 2026. Tags: WordPress, Local SEO, Google Business Profile, GA4, Search Console, Conversion design.*

```
I rebuilt the homepage for a used oil collection company that operates in 28 states, and I run their SEO program: local SEO, Google Business Profile listings and lead tracking.
```

#### Clox
`src/data/projects.ts → clox.summary`
*Card summary. Role: Founder. Year: 2026 to present. Tags: Next.js, TypeScript, Mobile apps, SaaS, SEO, Product marketing.*

```
My own product. Clox is a time-tracking app for small field crews in the trades, with native iOS and Android apps, a web dashboard and a marketing site. I designed, built and market it myself.
```

#### Keyset
`src/data/projects.ts → keyset.summary`
*Card summary. Role: Design and development. Year: 2026. Tags: React, TypeScript, Vite, Vitest, Playwright, Accessibility.*

```
My own project: a maintenance desk for a property management company, built so I had a React and TypeScript application with real state in it. Filtering and bulk edits, a schedule board that works by keyboard as well as by dragging, and optimistic saves that roll back when the server refuses. The data is generated and there is no client.
```

#### Explainer Visuals
`src/data/projects.ts → explainer-visuals.summary`
*Card summary. Role: Design and development. Year: 2024. Tags: Astro, HLS video, Figma, Shell scripting.*

```
I built a portfolio website for a video production studio where every video plays automatically. I used Astro and converted the videos to HLS playlists so the videos could load without serving the original files.
```

#### LoopManager
`src/data/projects.ts → loopmanager.summary`
*Card summary. Role: Redesign and custom CMS. Year: 2024. Tags: Astro, Figma, Custom CMS, HLS video.*

```
I redesigned the website for nuVector's LoopManager software and moved it from WordPress to Astro to match a new explainer video. I also built a custom blog CMS so the marketing team could publish posts without a developer.
```

#### nuVector
`src/data/projects.ts → nuvector.summary`
*Card summary. Role: Company website redesign. Year: 2024 to 2025. Tags: Astro, HTML & CSS, JavaScript, Figma, SEO.*

```
I redesigned and rebuilt nuVector's company website on Astro, with services, client logos, case studies and an SEO blog. The main call to action is a demo request.
```

#### Jonathan Zalzman Guitar
`src/data/projects.ts → jonathan-zalzman.summary`
*Card summary. Role: Design and development. Year: 2026. Tags: HTML & CSS, JavaScript, Netlify Forms, Local business.*

```
I built a website for a guitar teacher in Suwanee, Georgia. It covers the nine styles he teaches, his pricing, student reviews and a form to book a free trial lesson.
```

#### Landscaper Templates
`src/data/projects.ts → landscaper-templates.summary`
*Card summary. Role: Website templates. Year: 2026. Tags: HTML & CSS, JavaScript, Netlify, Templates.*

```
I built three website templates for landscaping companies. They are intentionally generic so a business owner can compare the layouts and the speed first. I then customize the one they choose.
```

#### Panda's Playhouse
`src/data/projects.ts → panda.summary`
*Card summary. Role: Wix Studio build. Year: 2023. Tags: Wix Studio, JavaScript (Velo), Third-party APIs, Figma.*

```
I built a membership website for an online gaming community with more than 5,000 members using Wix Studio and Velo. It includes facial verification for new sign-ups, a members-only area and a virtual currency system.
```

#### MMA Class Tracker
`src/data/projects.ts → 3.summary`
*Card summary. Role: Side project. Year: 2023. Tags: JavaScript, Firebase, Netlify.*

```
I built a small web app my friends and I use to track martial arts classes. Attendance earns experience points and levels, and progress syncs across devices with Firebase.
```

---

## 3. Project case study pages

### TimeClick

**Page:** `/web-developer-project-timeclick`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: TimeClick (Utah, US) · kind: Website redesign and marketing · role: Front-end developer and marketing specialist · year: 2024 to present  
**Tags:** WordPress, JavaScript, Zoho CRM, Stripe and WooCommerce, SEO, Email marketing  
**Tools:** WordPress, HTML & CSS, Tailwind CSS, JavaScript, Zoho CRM, WooCommerce, Stripe, Figma, Google Analytics 4, Search Console, Ahrefs, SEO, Email marketing  

#### summary
`src/data/project-walkthroughs.ts → timeclick.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
I redesigned the marketing site for a time clock software company, built the CRM, checkout and email systems behind it, and ran the SEO program that took organic traffic from about 4,000 to over 30,000 visitors a month.
```

#### description
`src/data/project-walkthroughs.ts → timeclick.description`
*One or two sentences directly under the page title.*

```
I redesigned timeclick.com, built the CRM, checkout and email systems behind it, and ran the SEO program that took organic traffic from about 4,000 to over 30,000 visitors a month.
```

#### pageDescription
`src/data/project-walkthroughs.ts → timeclick.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
TimeClick case study: the website redesign, the CRM, checkout and email systems behind it, and the SEO program that grew organic traffic from about 4,000 to over 30,000 monthly visitors between December 2025 and April 2026.
```

#### facts[0]
`src/data/project-walkthroughs.ts → timeclick.facts[0]`
*Label in the facts strip near the top of the page.*

```
SEO program: December 2025 to April 2026
```

#### heroCaption
`src/data/project-walkthroughs.ts → timeclick.heroCaption`
*Caption under the first screenshot.*

```
The first screen of the homepage at 1680 pixels wide.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → timeclick.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
4,000 to 30,000+ — Monthly organic visitors, December 2025 to April 2026 (Google Analytics)
```

#### highlights[1]
`src/data/project-walkthroughs.ts → timeclick.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Full redesign — Homepage, feature pages, pricing and the free trial flow
```

#### highlights[2]
`src/data/project-walkthroughs.ts → timeclick.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Email campaigns — Launched the trial and post-trial sequences, plus Zoho CRM, WooCommerce and Stripe
```

#### overviewTitle
`src/data/project-walkthroughs.ts → timeclick.overviewTitle`
*Heading above the overview paragraphs.*

```
What the site has to do
```

#### overview[0]
`src/data/project-walkthroughs.ts → timeclick.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
TimeClick sells desktop time clock software to small businesses. Unlike most competitors, it is sold for a one-time price with no subscription. The website has to explain that quickly, show that the product is trusted, and get the right visitors to start the 14-day free trial, which does not need a credit card. I redesigned the site with that goal, and then ran the SEO program that brought the visitors to it.
```

#### overview[1]
`src/data/project-walkthroughs.ts → timeclick.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The homepage starts with the offer and the trial button, followed by the proof: a 4.7 out of 5 rating from more than 1,000 reviews, customer logos, and a feature section that covers time tracking, PTO, job codes, reports, mobile apps, geofencing and payroll integrations with QuickBooks, ADP and Gusto. Reviews and a second trial button close the page. The site is responsive and built to load quickly on the laptops and phones that small business owners use.
```

#### overview[2]
`src/data/project-walkthroughs.ts → timeclick.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
I also built the systems around the site. I integrated a Zoho CRM newsletter signup into the blog. The original embed was being broken by WordPress content filters, so I replaced it with a plain JavaScript <code>fetch()</code> call. I fixed a WooCommerce and Stripe ACH microdeposit problem on the purchase site with a small script. I wrote and launched the trial and post-trial email campaigns, and the first newsletter to the existing customer base.
```

#### overview[3]
`src/data/project-walkthroughs.ts → timeclick.overview[3]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Separately from the website, I led a screen-by-screen redesign of the TimeClick 25 admin interface and delivered interactive HTML, CSS and JavaScript mockups that the development team built from.
```

#### results.title
`src/data/project-walkthroughs.ts → timeclick.results.title`
*Heading of the results block.*

```
SEO results, December 2025 to April 2026
```

#### results.intro
`src/data/project-walkthroughs.ts → timeclick.results.intro`
*Intro line of the results block.*

```
I ran the SEO program for five months and handed it off in April 2026. The numbers below come from Google Analytics and from Ahrefs.
```

#### results.stats[0]
`src/data/project-walkthroughs.ts → timeclick.results.stats[0]`
*Explaining line under a results stat.*

```
4,000 — Monthly organic visitors in December 2025 (Google Analytics)
```

#### results.stats[1]
`src/data/project-walkthroughs.ts → timeclick.results.stats[1]`
*Explaining line under a results stat.*

```
30,000+ — Monthly organic visitors in April 2026 (Google Analytics)
```

#### results.stats[2]
`src/data/project-walkthroughs.ts → timeclick.results.stats[2]`
*Explaining line under a results stat.*

```
2,400 — Keywords ranking, 472 of them in the top 3 (Ahrefs, September 2026)
```

#### results.stats[3]
`src/data/project-walkthroughs.ts → timeclick.results.stats[3]`
*Explaining line under a results stat.*

```
753 — Google AI Overviews that cite timeclick.com (Ahrefs, September 2026)
```

#### results.figures[0].caption
`src/data/project-walkthroughs.ts → timeclick.results.figures[0].caption`
*Caption under a results chart or screenshot.*

```
Estimated organic traffic to timeclick.com, September 2025 to September 2026 (traffic in orange, impressions in dark red). The climb starts in January 2026, the second month of the program, and holds after the program ended. Source: Ahrefs Site Explorer.
```

#### results.figures[1].caption
`src/data/project-walkthroughs.ts → timeclick.results.figures[1].caption`
*Caption under a results chart or screenshot.*

```
The pages that carry the traffic: the free tools built for search (the federal holidays calendar, the hours to decimal calculator), the tools index and the time management quotes page. Estimated monthly visits per page. Source: Ahrefs.
```

#### results.figures[2].caption
`src/data/project-walkthroughs.ts → timeclick.results.figures[2].caption`
*Caption under a results chart or screenshot.*

```
The searches that bring the most visitors are questions about holidays, answered by the federal holidays page. Source: Ahrefs.
```

#### results.paragraphs[0]
`src/data/project-walkthroughs.ts → timeclick.results.paragraphs[0]`
*Paragraph in the results block.*

```
The work: free tools and resource pages built for searches people make every day (a federal holidays calendar, a time card calculator, an hours to decimal calculator, a military time converter and others), articles for searches like "easy to use time clock software", on-page fixes across the site, schema markup fixes (the site was marked up as a local business; it is a national software company), new backlinks, and internal links from the tools pages to the product pages and the free trial.
```

#### results.paragraphs[1]
`src/data/project-walkthroughs.ts → timeclick.results.paragraphs[1]`
*Paragraph in the results block.*

```
Most of the new traffic is informational: people looking up holidays or converting hours. The tools pages link to the product and the trial, so that traffic feeds the top of the funnel. Ahrefs shows the same picture from the outside: estimated organic traffic went from about 2,000 visits a month in late 2025 to a peak near 45,000 in April 2026, and has stayed between roughly 20,000 and 40,000 since. Ahrefs numbers are estimates; the Google Analytics figures above are the measured ones.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → timeclick.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The homepage, top to bottom.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → timeclick.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → timeclick.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The offer in one line: time clock software with zero monthly fees. One orange button to start the trial, the 4.7 rating with the review sites it comes from, and a screenshot of the actual software with a play button for the product video.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → timeclick.sections[1].title`
*Short name of one section of the page being walked through.*

```
Proof: 8,000+ businesses
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → timeclick.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Customer logos right under the hero, before any feature talk. Best Western, Farmers Insurance, Holiday Inn, Nationwide, Ace Hardware, H&R Block and Hampton. Small businesses recognize them and relax.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → timeclick.sections[2].title`
*Short name of one section of the page being walked through.*

```
Made for small businesses
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → timeclick.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The first of three alternating rows. A short statement about who the product is for, a button to the reviews, and a custom illustration instead of a stock photo.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → timeclick.sections[3].title`
*Short name of one section of the page being walked through.*

```
Buy once, use forever
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → timeclick.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The main difference from every competitor gets its own row: you pay once and own the license. The button goes to pricing, which is where a visitor who likes this idea wants to go next.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → timeclick.sections[4].title`
*Short name of one section of the page being walked through.*

```
Payroll integrations
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → timeclick.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The third row answers the practical question: does it work with my payroll? QuickBooks, ADP and Gusto logos, one sentence, one button to the integrations page.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → timeclick.sections[5].title`
*Short name of one section of the page being walked through.*

```
Eight features, one grid
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → timeclick.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Every feature in two rows of four: time clock, employee time tracking, department and job tracking, timesheet reports, PTO, the mobile app, payroll integrations and geofencing. Each one is two sentences and a link to its own page, which is also where the SEO traffic for those terms lands.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → timeclick.sections[6].title`
*Short name of one section of the page being walked through.*

```
Works the way you do
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → timeclick.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Four objections handled in four short blocks: setup takes under ten minutes, support is real people in the US, time theft, and your data stays on your own computer or server instead of the cloud.
```

#### sections[7].title
`src/data/project-walkthroughs.ts → timeclick.sections[7].title`
*Short name of one section of the page being walked through.*

```
Reviews
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → timeclick.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three real reviews with names, roles and companies, from a plastics company, a dental practice and an imaging business. The blue panel breaks up the white page before the last call to action.
```

#### sections[8].title
`src/data/project-walkthroughs.ts → timeclick.sections[8].title`
*Short name of one section of the page being walked through.*

```
Last call to action and footer
```

#### sections[8].caption
`src/data/project-walkthroughs.ts → timeclick.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The trial offer again, with the two facts that matter (14 days, no credit card) above the headline. The footer lists every product page and every free tool, which also gives those pages internal links from every page on the site.
```

#### galleries[0].title
`src/data/project-walkthroughs.ts → timeclick.galleries[0].title`
*Heading of an image grid.*

```
Pages built for search and for the trial
```

#### galleries[0].intro
`src/data/project-walkthroughs.ts → timeclick.galleries[0].intro`
*Intro line of an image grid.*

```
A few of the other pages I designed and wrote: the pages that rank, and the pages that convert.
```

#### galleries[0].items[0].caption
`src/data/project-walkthroughs.ts → timeclick.galleries[0].items[0].caption`
*Caption under one image in a grid.*

```
US Federal Holidays Calendar. The single biggest source of organic traffic on the site, about 12,700 estimated visits a month.
```

#### galleries[0].items[1].caption
`src/data/project-walkthroughs.ts → timeclick.galleries[0].items[1].caption`
*Caption under one image in a grid.*

```
Free Time Card Calculator. A working tool with breaks, lunch and overtime, built for the searches small business owners make every payday.
```

#### galleries[0].items[2].caption
`src/data/project-walkthroughs.ts → timeclick.galleries[0].items[2].caption`
*Caption under one image in a grid.*

```
Pricing. One-time price per device, what is included, and what a renewal costs, with nothing hidden.
```

#### galleries[0].items[3].caption
`src/data/project-walkthroughs.ts → timeclick.galleries[0].items[3].caption`
*Caption under one image in a grid.*

```
No subscription. A page built around the one argument competitors cannot make, with a cost comparison over time.
```

#### galleries[0].items[4].caption
`src/data/project-walkthroughs.ts → timeclick.galleries[0].items[4].caption`
*Caption under one image in a grid.*

```
The free trial page. Everything on the site points here: one short form, no credit card, no distractions.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → timeclick.phonesIntro`
*One line introducing the phone screenshots.*

```
The homepage on a phone. One column, the trial button within reach of a thumb, and the feature list turned into cards.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → timeclick.phones[0].caption`
*Caption under one phone screenshot.*

```
The hero: headline, button and rating fit on the first screen.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → timeclick.phones[1].caption`
*Caption under one phone screenshot.*

```
The feature grid becomes one column of cards.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → timeclick.phones[2].caption`
*Caption under one phone screenshot.*

```
The objections section, stacked with the icons centered.
```

#### phones[3].caption
`src/data/project-walkthroughs.ts → timeclick.phones[3].caption`
*Caption under one phone screenshot.*

```
Reviews stack as full-width cards.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → timeclick.details.design[0]`
*Bullet in the Design notes list.*

```
One message at the top: no monthly fees, free trial, no credit card. Everything else on the page supports that.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → timeclick.details.design[1]`
*Bullet in the Design notes list.*

```
Proof before features: the rating, the review count and the customer logos come right after the hero.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → timeclick.details.design[2]`
*Bullet in the Design notes list.*

```
Feature sections built around screenshots of the real software, not illustrations.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → timeclick.details.design[3]`
*Bullet in the Design notes list.*

```
Large text and high contrast, because the buyers are small business owners on laptops and phones, not designers on big screens.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → timeclick.details.build[0]`
*Bullet in the Build notes list.*

```
WordPress child theme with hand-coded page templates, styled with Tailwind CSS. No page builder on the pages I built.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → timeclick.details.build[1]`
*Bullet in the Build notes list.*

```
A Zoho CRM signup on the blog rebuilt as a plain JavaScript <code>fetch()</code> call after WordPress filters kept breaking the embed.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → timeclick.details.build[2]`
*Bullet in the Build notes list.*

```
A small script that fixed the WooCommerce and Stripe ACH microdeposit flow on the purchase site.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → timeclick.details.build[3]`
*Bullet in the Build notes list.*

```
Free tools pages (calendar, calculators, converters) built as their own post type, each with FAQ and WebApplication schema.
```

#### details.marketing[0]
`src/data/project-walkthroughs.ts → timeclick.details.marketing[0]`
*Bullet in the Marketing notes list.*

```
Five-month SEO program: tools and resource pages, articles, on-page fixes, schema fixes, backlinks and internal linking. Organic traffic went from about 4,000 to over 30,000 visitors a month.
```

#### details.marketing[1]
`src/data/project-walkthroughs.ts → timeclick.details.marketing[1]`
*Bullet in the Marketing notes list.*

```
Launched the trial and post-trial email campaigns, and the first newsletter to the existing customer base.
```

#### details.marketing[2]
`src/data/project-walkthroughs.ts → timeclick.details.marketing[2]`
*Bullet in the Marketing notes list.*

```
Monthly reporting from Google Analytics 4, Search Console and Ahrefs.
```

---

### Universal Environmental Services

**Page:** `/web-developer-project-ues`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: Universal Environmental Services (Georgia, US) · kind: Homepage rebuild and SEO · role: Web developer and SEO consultant · year: 2026 to present  
**Tags:** WordPress, Local SEO, Google Business Profile, GA4, Search Console, Conversion design  
**Tools:** WordPress, HTML & CSS, JavaScript, WPForms, Google Business Profile, Google Analytics 4, Search Console, Local SEO  

#### summary
`src/data/project-walkthroughs.ts → ues.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
I rebuilt the homepage for a used oil collection company that operates in 28 states. I am now rebuilding the rest of the site page by page while running its local SEO program.
```

#### description
`src/data/project-walkthroughs.ts → ues.description`
*One or two sentences directly under the page title.*

```
I rebuilt the homepage for a used oil collection and recycling company that operates in 28 states. I am rebuilding the rest of the site on the same template and running its SEO program.
```

#### pageTitle
`src/data/project-walkthroughs.ts → ues.pageTitle`
*Browser tab title and search result headline.*

```
Universal Environmental Services case study | Manny Pachano
```

#### pageDescription
`src/data/project-walkthroughs.ts → ues.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
Universal Environmental Services case study: the homepage rebuild shown before and after and section by section, the mobile version, and the local SEO program behind it.
```

#### facts[0]
`src/data/project-walkthroughs.ts → ues.facts[0]`
*Label in the facts strip near the top of the page.*

```
Status: Homepage live; other pages being rebuilt
```

#### compare.labelA
`src/data/project-walkthroughs.ts → ues.compare.labelA`
*Label on the left side of the before/after slider.*

```
Before
```

#### compare.labelB
`src/data/project-walkthroughs.ts → ues.compare.labelB`
*Label on the right side of the before/after slider.*

```
After
```

#### heroCaption
`src/data/project-walkthroughs.ts → ues.heroCaption`
*Caption under the first screenshot.*

```
The first screen before and after. Drag the handle. The old page opened with a photo and a phone number; the new one opens with what the company does, where, and two ways to get a pickup.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → ues.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
28 states — Service area covered by the local SEO program
```

#### highlights[1]
`src/data/project-walkthroughs.ts → ues.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One template — The homepage pattern the rest of the site is now being rebuilt on
```

#### highlights[2]
`src/data/project-walkthroughs.ts → ues.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Lead tracking — Form submissions and phone calls tracked in Google Analytics 4
```

#### overviewTitle
`src/data/project-walkthroughs.ts → ues.overviewTitle`
*Heading above the overview paragraphs.*

```
What the site has to do
```

#### overview[0]
`src/data/project-walkthroughs.ts → ues.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
UES collects used oil, filters, antifreeze and oily water from more than 35,000 businesses and re-refines the oil into new base oil at its plant in Peachtree City, Georgia. Its customers are shop owners and fleet managers. They want to know that a truck will show up and that the paperwork will pass an inspection. The old homepage did not answer either question quickly, so I rebuilt it with both answers near the top of the page.
```

#### overview[1]
`src/data/project-walkthroughs.ts → ues.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The new homepage is the first page built on a template that the rest of the site is being rebuilt on; the hazardous waste page is the second. It starts with a short headline, a call button and a request-a-pickup button with a same-day reply promise, followed by the company's numbers. The nine collection services each get a short card. Below that are a four-step explanation of how a pickup works, a compliance section (a receipt at every pickup, EPA transporter ID, insurance and industry memberships), a section about the re-refinery, a list of states, customer quotes and a pickup request form. The page uses large text and high contrast and was designed for phones first.
```

#### overview[2]
`src/data/project-walkthroughs.ts → ues.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The second part of the engagement is SEO. I run the technical and local SEO work across the 28-state service area, including the company's Google Business Profile listings (verification, cleanup and appeals), on-page SEO as each page is rebuilt, and content written around how customers search for pickup services in their area. I set up Google Analytics 4 and Search Console so that form submissions and phone clicks are tracked as leads, and I send a monthly report that shows rankings, traffic and leads by location. The program is in its second month, so there are no results to show yet.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → ues.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The new homepage, top to bottom.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → ues.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header, hero and the numbers
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → ues.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The headline says what the company does and where. Two buttons: request a pickup, or call. Under them, the promise that matters to a shop owner: same business day reply, no account needed. The yellow strip carries the four numbers that make the company credible.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → ues.sections[1].title`
*Short name of one section of the page being walked through.*

```
Nine services
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → ues.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One card per service, each with an icon, two sentences and a link to its page. The black bar at the bottom catches the visitor who is not sure what category their waste falls into, which is a common reason people do not call.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → ues.sections[2].title`
*Short name of one section of the page being walked through.*

```
Four steps, start to finish
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → ues.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
What happens after you call, in four numbered steps: tell us what you have, we put you on a route, our driver pumps and hauls, you get the receipt. The old site never explained this.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → ues.sections[3].title`
*Short name of one section of the page being walked through.*

```
Compliance
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → ues.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The inspector question, answered: a receipt at every pickup, the full service history in the customer portal, safety data sheets, a $20M insurance policy and the industry memberships. The receipt on the right is drawn in HTML and CSS, not a screenshot.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → ues.sections[4].title`
*Short name of one section of the page being walked through.*

```
The re-refinery
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → ues.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A dark section about what happens to the oil after pickup. The yellow card carries the two numbers that matter: 50 to 85 percent less energy than making base oil from crude, and one re-refinery in Peachtree City.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → ues.sections[5].title`
*Short name of one section of the page being walked through.*

```
Find your state
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → ues.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A map of the service area and a list of the 28 states. Each state links to its own page, which is where the local SEO work lands.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → ues.sections[6].title`
*Short name of one section of the page being walked through.*

```
Customer quotes
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → ues.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three unedited comments from customers on the routes, with the town and state. Short, specific and believable.
```

#### sections[7].title
`src/data/project-walkthroughs.ts → ues.sections[7].title`
*Short name of one section of the page being walked through.*

```
The environmental program
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → ues.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One short paragraph and a link to the full program page, next to a photo of the plant.
```

#### sections[8].title
`src/data/project-walkthroughs.ts → ues.sections[8].title`
*Short name of one section of the page being walked through.*

```
Pickup request form and footer
```

#### sections[8].caption
`src/data/project-walkthroughs.ts → ues.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The form asks only what the dispatcher needs: who you are, where you are, what to collect and roughly how much. The phone number is right next to it for people who would rather talk. Submissions and calls are tracked as leads in GA4.
```

#### galleries[0].title
`src/data/project-walkthroughs.ts → ues.galleries[0].title`
*Heading of an image grid.*

```
The next pages on the same template
```

#### galleries[0].intro
`src/data/project-walkthroughs.ts → ues.galleries[0].intro`
*Intro line of an image grid.*

```
The rest of the site is being rebuilt page by page on the homepage template. The hazardous waste division page was the first one after the homepage.
```

#### galleries[0].items[0].caption
`src/data/project-walkthroughs.ts → ues.galleries[0].items[0].caption`
*Caption under one image in a grid.*

```
Hazardous waste removal and disposal. Same header, same buttons, an on-this-page list for a longer page.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → ues.phonesIntro`
*One line introducing the phone screenshots.*

```
The new homepage on a phone. It was designed for phones first, because that is what a shop owner has in hand when the drum is full.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → ues.phones[0].caption`
*Caption under one phone screenshot.*

```
Headline, two full-width buttons and the reply promise on the first screen.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → ues.phones[1].caption`
*Caption under one phone screenshot.*

```
The four steps, stacked.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → ues.phones[2].caption`
*Caption under one phone screenshot.*

```
The compliance checklist stays readable at phone size.
```

#### phones[3].caption
`src/data/project-walkthroughs.ts → ues.phones[3].caption`
*Caption under one phone screenshot.*

```
The phone number comes before the form, because most people on a phone would rather call.
```

#### phonesB.intro
`src/data/project-walkthroughs.ts → ues.phonesB.intro`
*One line introducing the phone screenshots.*

```
The old homepage on a phone, for comparison: a photo, a phone number, a welcome paragraph, and nine magnifying-glass icons for the services.
```

#### phonesB.items[0].caption
`src/data/project-walkthroughs.ts → ues.phonesB.items[0].caption`
*Caption under one phone screenshot.*

```
Before: the first screen was a photo and a phone number.
```

#### phonesB.items[1].caption
`src/data/project-walkthroughs.ts → ues.phonesB.items[1].caption`
*Caption under one phone screenshot.*

```
Before: the services, each with the same magnifying-glass icon.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → ues.details.design[0]`
*Bullet in the Design notes list.*

```
Two questions answered above the fold: will a truck show up, and will the paperwork pass an inspection.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → ues.details.design[1]`
*Bullet in the Design notes list.*

```
Black, white and the company yellow. Large text and high contrast for people reading on a phone in a shop.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → ues.details.design[2]`
*Bullet in the Design notes list.*

```
Numbers wherever they exist: customers, trucks, gallons, states, energy saved.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → ues.details.design[3]`
*Bullet in the Design notes list.*

```
The sample receipt is drawn in HTML and CSS, so it is sharp on every screen and weighs almost nothing.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → ues.details.build[0]`
*Bullet in the Build notes list.*

```
WordPress with a hand-coded page template in the child theme, so the rest of the site can be rebuilt on it page by page.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → ues.details.build[1]`
*Bullet in the Build notes list.*

```
WPForms for the pickup request form, with Cloudflare Turnstile instead of a captcha puzzle.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → ues.details.build[2]`
*Bullet in the Build notes list.*

```
Form submissions and phone clicks tracked as leads in Google Analytics 4.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → ues.details.build[3]`
*Bullet in the Build notes list.*

```
An interactive map of the service area; every state links to its own page.
```

#### details.marketing[0]
`src/data/project-walkthroughs.ts → ues.details.marketing[0]`
*Bullet in the Marketing notes list.*

```
Local SEO across 28 states: Google Business Profile verification, cleanup and appeals, on-page SEO as each page is rebuilt, and content written around how customers search for pickup services in their area.
```

#### details.marketing[1]
`src/data/project-walkthroughs.ts → ues.details.marketing[1]`
*Bullet in the Marketing notes list.*

```
Google Analytics 4 and Search Console set up for lead tracking, with a monthly report by location.
```

#### details.marketing[2]
`src/data/project-walkthroughs.ts → ues.details.marketing[2]`
*Bullet in the Marketing notes list.*

```
Second month of the program. Results will be added here when there are enough months to show a trend.
```

---

### Clox

**Page:** `/web-developer-project-clox`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: Clox (my own company) · kind: Founder · role: Founder: product, design, front end and marketing · year: 2026 to present · liveLabel: Visit getclox.com  
**Tags:** Next.js, TypeScript, Mobile apps, SaaS, SEO, Product marketing  
**Tools:** Next.js, TypeScript, React, Native mobile apps, Figma, SEO, Product Hunt  

#### summary
`src/data/project-walkthroughs.ts → clox.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
I built Clox for small field crews in the trades: native iOS and Android apps, a web dashboard and the marketing site. I designed, built and market the whole product.
```

#### description
`src/data/project-walkthroughs.ts → clox.description`
*One or two sentences directly under the page title.*

```
Clox is my own time-tracking product for small field crews in the trades. I designed and built the native apps, web dashboard and marketing site, and I handle the marketing.
```

#### pageDescription
`src/data/project-walkthroughs.ts → clox.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
Clox case study: the getclox.com marketing site section by section, the product screens, the industry and comparison pages, and the mobile version.
```

#### heroCaption
`src/data/project-walkthroughs.ts → clox.heroCaption`
*Caption under the first screenshot.*

```
The first screen of getclox.com at 1680 pixels wide.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → clox.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
$29 a month — Includes three users, then $6 per user
```

#### highlights[1]
`src/data/project-walkthroughs.ts → clox.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
iOS, Android and web — Native apps for the crew and a web dashboard for the office
```

#### highlights[2]
`src/data/project-walkthroughs.ts → clox.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
14-day trial — No credit card, no sales call, no onboarding fee
```

#### overviewTitle
`src/data/project-walkthroughs.ts → clox.overviewTitle`
*Heading above the overview paragraphs.*

```
What the site has to do
```

#### overview[0]
`src/data/project-walkthroughs.ts → clox.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Many small trades crews (electricians, plumbers, HVAC, landscapers, general contractors) still track hours on paper timecards or in text messages. The hours get collected at the end of the week and the totals are often wrong. Clox records hours at the time they happen. The crew clocks in from their phones, and the office sees hours, job costs and the schedule in one place. I founded the company and I am responsible for the product, the design, the front end and the marketing.
```

#### overview[1]
`src/data/project-walkthroughs.ts → clox.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The product is a native iOS and Android app for workers and a web dashboard for managers. Clock-ins use GPS and geofencing, so a clock-in from outside the job site is blocked. Overtime and break rules are applied automatically, each job shows its labor cost, and payroll can be exported to QuickBooks, ADP, Gusto or Paychex. Managers can build schedules by drag and drop, approve shifts and time-off requests, and see the day's clock-ins on a map. Time records can be exported as a signed PDF if there is a dispute.
```

#### overview[2]
`src/data/project-walkthroughs.ts → clox.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The marketing site has one job: get a crew owner to start the free trial. The homepage follows the structure I use for client sites: the problem, the fix, the features, a tour of the real product, three steps to start, a list of the trades it is built for, the price and one call to action. Around it sit a page for each trade, a comparison page for each competitor, docs and a blog, all written from search data.
```

#### overview[3]
`src/data/project-walkthroughs.ts → clox.overview[3]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Running my own product means I deal with positioning, pricing, onboarding, SEO and support directly. That experience carries over to the client sites I build.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → clox.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The homepage, top to bottom.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → clox.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → clox.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One headline that names the problem, one paragraph that says who it is for, one button. The app store badges and a 40-second tour link sit under it for people who want to see before they sign up. The two phones are the clock-in screen rebuilt in HTML and CSS, so they are sharp on every display and cost nothing to load.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → clox.sections[1].title`
*Short name of one section of the page being walked through.*

```
The problem, then the fix
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → clox.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A short section on why paper timecards cost money, in the words a crew owner would use, and a boxed one-line answer: every punch lands the moment it happens.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → clox.sections[2].title`
*Short name of one section of the page being walked through.*

```
Six features
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → clox.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Two rows of three cards: clock in even offline, off-site clock-ins blocked, overtime and breaks calculated, real job costs, records that hold up in a dispute, fix a punch. Each card is two or three sentences, no icons, no marketing words.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → clox.sections[3].title`
*Short name of one section of the page being walked through.*

```
The 40-second tour
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → clox.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A real screen recording of a clock-in, 40 seconds, with captions. It is the only media file on the page, it loads on demand and it does not autoplay.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → clox.sections[4].title`
*Short name of one section of the page being walked through.*

```
Product tour
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → clox.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Four alternating rows, each a dashboard screen drawn in HTML and CSS: reports with overtime flagged before payday, the team page with who is on the clock, the weekly schedule, and the map of clock-ins. Ends with the trial button again.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → clox.sections[5].title`
*Short name of one section of the page being walked through.*

```
Three steps to start
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → clox.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Set up on the web, invite the crew by text, run payroll on Friday. Numbered 01, 02, 03, one sentence each. No sales call, no onboarding fee.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → clox.sections[6].title`
*Short name of one section of the page being walked through.*

```
Is this you?
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → clox.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Eight trades as small cards: electricians, plumbers, HVAC, landscapers, general contractors, cleaning services, concrete and masonry, roofers. The site has a page written for each of 20 trades, linked from the footer and the trades index.
```

#### sections[7].title
`src/data/project-walkthroughs.ts → clox.sections[7].title`
*Short name of one section of the page being walked through.*

```
Pricing
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → clox.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One price, in one line: $29 a month for the first three users, then $6 per user. Every feature is on every plan. The trial needs no credit card.
```

#### sections[8].title
`src/data/project-walkthroughs.ts → clox.sections[8].title`
*Short name of one section of the page being walked through.*

```
Last call to action and footer
```

#### sections[8].caption
`src/data/project-walkthroughs.ts → clox.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
"Get your Sunday night back." The trial button and the how-it-works link one more time, then a footer that links the main trade pages, the five comparison pages and the legal pages.
```

#### galleries[0].title
`src/data/project-walkthroughs.ts → clox.galleries[0].title`
*Heading of an image grid.*

```
The product
```

#### galleries[0].intro
`src/data/project-walkthroughs.ts → clox.galleries[0].intro`
*Intro line of an image grid.*

```
The screens behind the site: the web dashboard for the office and the app for the crew.
```

#### galleries[0].items[0].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[0].caption`
*Caption under one image in a grid.*

```
Hours by employee in the dashboard, and the clock-in screen on the phone.
```

#### galleries[0].items[1].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[1].caption`
*Caption under one image in a grid.*

```
One-tap clock-in. Outside the job site, the punch is blocked.
```

#### galleries[0].items[2].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[2].caption`
*Caption under one image in a grid.*

```
Every clock-in on a map of the job sites.
```

#### galleries[0].items[3].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[3].caption`
*Caption under one image in a grid.*

```
Drag-and-drop scheduling, seen by the crew on their phones.
```

#### galleries[0].items[4].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[4].caption`
*Caption under one image in a grid.*

```
Shift and time-off approvals.
```

#### galleries[0].items[5].caption
`src/data/project-walkthroughs.ts → clox.galleries[0].items[5].caption`
*Caption under one image in a grid.*

```
Overtime flagged before payroll, with the labor cost total.
```

#### galleries[1].title
`src/data/project-walkthroughs.ts → clox.galleries[1].title`
*Heading of an image grid.*

```
The pages around the homepage
```

#### galleries[1].intro
`src/data/project-walkthroughs.ts → clox.galleries[1].intro`
*Intro line of an image grid.*

```
Each trade has its own page, each competitor has a comparison page, and the pricing and how-it-works pages answer the two questions every trial signup asks first.
```

#### galleries[1].items[0].caption
`src/data/project-walkthroughs.ts → clox.galleries[1].items[0].caption`
*Caption under one image in a grid.*

```
Pricing: one plan, monthly or annual.
```

#### galleries[1].items[1].caption
`src/data/project-walkthroughs.ts → clox.galleries[1].items[1].caption`
*Caption under one image in a grid.*

```
How it works, split into managers and crews.
```

#### galleries[1].items[2].caption
`src/data/project-walkthroughs.ts → clox.galleries[1].items[2].caption`
*Caption under one image in a grid.*

```
One of 20 trade pages, written for electrical contractors.
```

#### galleries[1].items[3].caption
`src/data/project-walkthroughs.ts → clox.galleries[1].items[3].caption`
*Caption under one image in a grid.*

```
One of five comparison pages, with a plain price and feature table.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → clox.phonesIntro`
*One line introducing the phone screenshots.*

```
getclox.com on a phone. Most crew owners will see it there first.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → clox.phones[0].caption`
*Caption under one phone screenshot.*

```
One column, the trial button above the fold, and the tour link for people who want to look first.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → clox.phones[1].caption`
*Caption under one phone screenshot.*

```
The problem in three sentences, then the one-line answer in its own box.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → clox.phones[2].caption`
*Caption under one phone screenshot.*

```
The feature grid becomes one column, in the same order.
```

#### phones[3].caption
`src/data/project-walkthroughs.ts → clox.phones[3].caption`
*Caption under one phone screenshot.*

```
The video and the dashboard rows, each one full width.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → clox.details.design[0]`
*Bullet in the Design notes list.*

```
Cream background, one dark accent and one red. The product screens are the visuals; there are no stock photos and no icons for the sake of icons.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → clox.details.design[1]`
*Bullet in the Design notes list.*

```
Every section answers one question a crew owner has, in the order they have them: what is this, why, what do I get, show me, how do I start, is it for me, what does it cost.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → clox.details.design[2]`
*Bullet in the Design notes list.*

```
Short copy in trade language. "Sunday night", "payroll Friday", "the drum is full" kind of language, not software language.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → clox.details.design[3]`
*Bullet in the Design notes list.*

```
Twenty trade pages and five comparison pages built on the same components as the homepage, so they stay consistent.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → clox.details.build[0]`
*Bullet in the Build notes list.*

```
Next.js and TypeScript for the marketing site. The product is native iOS and Android apps plus a web dashboard.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → clox.details.build[1]`
*Bullet in the Build notes list.*

```
There is not a single image file on the homepage: the phones, the dashboard windows and the map are drawn in HTML, CSS and SVG. The only media file is the 40-second video, and it loads on demand.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → clox.details.build[2]`
*Bullet in the Build notes list.*

```
No third-party scripts on the page. Every script comes from getclox.com.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → clox.details.build[3]`
*Bullet in the Build notes list.*

```
Docs, a blog, 20 trade pages and five comparison pages share the homepage components.
```

#### details.marketing[0]
`src/data/project-walkthroughs.ts → clox.details.marketing[0]`
*Bullet in the Marketing notes list.*

```
A blog and the trade and comparison pages are written from search data.
```

#### details.marketing[1]
`src/data/project-walkthroughs.ts → clox.details.marketing[1]`
*Bullet in the Marketing notes list.*

```
App store listings, directory profiles and a Product Hunt launch.
```

#### details.marketing[2]
`src/data/project-walkthroughs.ts → clox.details.marketing[2]`
*Bullet in the Marketing notes list.*

```
Running my own product means I handle positioning, pricing, onboarding, SEO and support directly.
```

---

### Keyset

**Page:** `/web-developer-project-keyset`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: My own project. No client, and the data is generated. · kind: React application · role: Design and development · year: 2026 · liveLabel: Open Keyset  
**Tags:** React, TypeScript, Vite, Vitest, Playwright, Accessibility  
**Tools:** React, TypeScript, Vite, Vitest, Testing Library, Playwright, axe, Netlify  

#### summary
`src/data/project-walkthroughs.ts → keyset.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
A maintenance desk for a property management company, built in React and TypeScript. Filtering, bulk edits, a schedule board that works by keyboard as well as by dragging, and optimistic saves that roll back when a save is refused.
```

#### description
`src/data/project-walkthroughs.ts → keyset.description`
*One or two sentences directly under the page title.*

```
A maintenance desk for a property management company: 200 requests across five properties, filtered, edited in bulk and scheduled on a week board. I built it because I wanted a React and TypeScript application with enough state in it to be worth testing. The data is generated and there is no client.
```

#### pageDescription
`src/data/project-walkthroughs.ts → keyset.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
Keyset case study: a React and TypeScript maintenance desk with filtering, bulk edits, a keyboard-operable schedule board and optimistic saves that roll back.
```

#### facts[0]
`src/data/project-walkthroughs.ts → keyset.facts[0]`
*Label in the facts strip near the top of the page.*

```
Source: github.com/MannyPachano/keyset
```

#### facts[1]
`src/data/project-walkthroughs.ts → keyset.facts[1]`
*Label in the facts strip near the top of the page.*

```
Tests: 132 across three suites
```

#### heroCaption
`src/data/project-walkthroughs.ts → keyset.heroCaption`
*Caption under the first screenshot.*

```
The queue. 200 requests across five properties, sorted by how long each has been open. The status chips carry live counts, and rows past their deadline are marked.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → keyset.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
132 tests — Logic, components, and browser tests covering four journeys and six accessibility checks
```

#### highlights[1]
`src/data/project-walkthroughs.ts → keyset.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
100 accessibility — Lighthouse, with axe run against four separate states of the app
```

#### highlights[2]
`src/data/project-walkthroughs.ts → keyset.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
No router, no CSS framework — The History API and hand-written CSS
```

#### overviewTitle
`src/data/project-walkthroughs.ts → keyset.overviewTitle`
*Heading above the overview paragraphs.*

```
Why I built it
```

#### overview[0]
`src/data/project-walkthroughs.ts → keyset.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
I had not shipped a React application, and reading about it was not going to fix that. So I picked a problem with enough state to be awkward: a maintenance desk where a property manager triages repair requests, assigns contractors and books visits.
```

#### overview[1]
`src/data/project-walkthroughs.ts → keyset.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The rule I set was that all the logic lives in plain modules with no React in them. Filtering, sorting, pagination, dates and the reducer are ordinary functions, so they run under the bare Node test runner with nothing installed. Only rendering needs a toolchain. That is why most of the test suite kept working while the interface changed.
```

#### overview[2]
`src/data/project-walkthroughs.ts → keyset.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Saves are optimistic: the row changes immediately and rolls back if the request fails. There is a Force failures button in the header so you can see that happen rather than take my word for it. The data comes from a fixed seed, so everyone sees the same 200 requests.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → keyset.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The app, screen by screen.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → keyset.sections[0].title`
*Short name of one section of the page being walked through.*

```
Filtering
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → keyset.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Search matches reference, unit, tenant and contractor, and every term has to match. The status chips carry live counts that ignore the status filter itself, so clicking Done shows you what Done contains rather than zero. Filters go into the URL, so a narrowed queue is a link you can send.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → keyset.sections[1].title`
*Short name of one section of the page being walked through.*

```
Selecting a range
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → keyset.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Click one checkbox, shift-click another, and everything between them is selected. The bar that appears assigns a contractor, sets a status or sets a priority for all of them at once. A change that pushes rows out of the current filter says so afterwards, with a button to show them.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → keyset.sections[2].title`
*Short name of one section of the page being walked through.*

```
One request
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → keyset.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Everything about a request in one panel: the property, the tenant, how long it has been open, and a history of every change with who made it. Opening it moves focus inside, Escape closes it, and Tab stays in the panel while it is open.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → keyset.sections[3].title`
*Short name of one section of the page being walked through.*

```
The week
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → keyset.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Scheduled visits laid out by day. Empty days stay on the board because they are drop targets. A day holding more visits than a crew can fit is flagged rather than quietly accepted.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → keyset.sections[4].title`
*Short name of one section of the page being walked through.*

```
Moving a visit without a mouse
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → keyset.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Each card has a handle. Enter picks the visit up, the arrow keys choose a day, Enter drops it and Escape puts it back. Every step is announced. Drag on its own would have made rescheduling a mouse-only feature.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → keyset.sections[5].title`
*Short name of one section of the page being walked through.*

```
When a save fails
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → keyset.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The row changes immediately and goes back exactly as it was if the request is rejected, field by field. The banner says what failed and that nothing was saved. Force failures in the header makes every save fail, so this is something you can try rather than something I claim.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → keyset.sections[6].title`
*Short name of one section of the page being walked through.*

```
Nothing to show
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → keyset.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
An empty queue and a filter that matches nothing are different situations, so they say different things. This one names the filters as the reason and offers to clear them.
```

#### sections[7].title
`src/data/project-walkthroughs.ts → keyset.sections[7].title`
*Short name of one section of the page being walked through.*

```
Dark theme
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → keyset.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The theme follows the operating system until you pick one, and the choice is remembered. A small file loaded before the first paint sets it, so the page never flashes light before turning dark. Colours run through tokens, and both themes were measured against the 4.5 to 1 contrast minimum.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → keyset.phonesIntro`
*One line introducing the phone screenshots.*

```
On a phone the table becomes cards. A nine-column table at 390 pixels is unreadable however it is scaled.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → keyset.phones[0].caption`
*Caption under one phone screenshot.*

```
Each row becomes a card with the reference, the property, the issue and the status. The checkbox moves to the corner and the overdue marker runs down the edge of the card.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → keyset.phones[1].caption`
*Caption under one phone screenshot.*

```
The detail panel fills the screen. The controls are the same ones as on desktop, at a size you can hit with a thumb.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → keyset.phones[2].caption`
*Caption under one phone screenshot.*

```
The week stacks one day per row, keeping the move handle and the visit count on every day.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → keyset.details.design[0]`
*Bullet in the Design notes list.*

```
Status and priority are never colour alone. Priority carries a dot and a word, so it still reads if you cannot separate red from amber.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → keyset.details.design[1]`
*Bullet in the Design notes list.*

```
One accent colour. Red is kept for overdue and for failures, so it means one thing.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → keyset.details.design[2]`
*Bullet in the Design notes list.*

```
Figures use tabular numerals, so columns of days and counts line up.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → keyset.details.design[3]`
*Bullet in the Design notes list.*

```
The bar under each age shows how much of the response window has been used: four hours for an emergency, two days for urgent, seven for routine.
```

#### details.design[4]
`src/data/project-walkthroughs.ts → keyset.details.design[4]`
*Bullet in the Design notes list.*

```
Every control is at least 24 pixels, and the ones used repeatedly on a phone are larger.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → keyset.details.build[0]`
*Bullet in the Build notes list.*

```
React 19 and TypeScript on Vite. No router: the view and the open request live in the query string and are set with the History API directly.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → keyset.details.build[1]`
*Bullet in the Build notes list.*

```
All logic sits in plain modules with no React imports, so 88 of the tests run under the bare Node test runner with nothing installed.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → keyset.details.build[2]`
*Bullet in the Build notes list.*

```
34 component tests in Vitest and Testing Library, and 10 browser tests in Playwright: four journeys and six accessibility checks.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → keyset.details.build[3]`
*Bullet in the Build notes list.*

```
The accessibility suite runs axe against the queue in both themes, the board, the open panel and a 390 pixel viewport, then measures tap targets and checks that nothing overlaps.
```

#### details.build[4]
`src/data/project-walkthroughs.ts → keyset.details.build[4]`
*Bullet in the Build notes list.*

```
A mock API with adjustable latency and failure rate, backed by localStorage, so the optimistic updates and the rollback have something real to fail against.
```

#### details.build[5]
`src/data/project-walkthroughs.ts → keyset.details.build[5]`
*Bullet in the Build notes list.*

```
Deployed on Netlify with a Content Security Policy that keeps script-src at self, which is why the theme script is a file rather than an inline tag.
```

---

### Landscaper Templates

**Page:** `/web-developer-project-landscaper-templates`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: My own project, for landscaping businesses · kind: Template system · role: Design and development · year: 2026 · liveLabel: See the templates  
**Tags:** HTML & CSS, JavaScript, Netlify, Templates  
**Tools:** HTML & CSS, JavaScript, Netlify, Figma, Photoshop  

#### summary
`src/data/project-walkthroughs.ts → landscaper-templates.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
Three ready-to-customize website templates for landscaping companies. Intentionally generic, so a business owner can see the layout, the flow and the speed before I make it theirs.
```

#### description
`src/data/project-walkthroughs.ts → landscaper-templates.description`
*One or two sentences directly under the page title.*

```
Three complete landscaping websites built as a sales tool. A business owner picks the layout they like, and I customize it with their name, photos and service area.
```

#### pageDescription
`src/data/project-walkthroughs.ts → landscaper-templates.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
Landscaper Templates case study: three landscaping website templates shown section by section and on a phone, and how the switcher and the customization work.
```

#### heroCaption
`src/data/project-walkthroughs.ts → landscaper-templates.heroCaption`
*Caption under the first screenshot.*

```
The first screen of template 1 at 1680 pixels wide. A bar at the bottom of the live page switches between the three templates and stays visible while you scroll.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → landscaper-templates.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Complete sites — Three finished websites to compare, not mockups or wireframes
```

#### highlights[1]
`src/data/project-walkthroughs.ts → landscaper-templates.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One switcher — Visitors move between templates without leaving the page
```

#### highlights[2]
`src/data/project-walkthroughs.ts → landscaper-templates.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Free customization — Included when a business owner chooses a template
```

#### overviewTitle
`src/data/project-walkthroughs.ts → landscaper-templates.overviewTitle`
*Heading above the overview paragraphs.*

```
What the pages have to do
```

#### overview[0]
`src/data/project-walkthroughs.ts → landscaper-templates.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Most landscaping business owners want to see what their website would look like before they commit to anything. Instead of showing mockups, I built three complete landscaping websites and put them behind a switcher that stays visible while you scroll, so an owner can compare them in a minute.
```

#### overview[1]
`src/data/project-walkthroughs.ts → landscaper-templates.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
All three cover the same ground: a headline that says what the company does and where, services, a gallery of past work, reasons to choose the company, customer reviews and a quote request. Each one orders and styles it differently, and each adds a section of its own (seasonal tips in one, a blog and a team page in another, service areas in the third), so an owner can pick the one that fits how they sell.
```

#### overview[2]
`src/data/project-walkthroughs.ts → landscaper-templates.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The templates are generic on purpose. The text and photos are placeholders. Once a business owner picks a template, I replace the placeholder content with their business name, photos and service area. That customization is free.
```

#### overview[3]
`src/data/project-walkthroughs.ts → landscaper-templates.overview[3]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The templates are hand-coded HTML, CSS and JavaScript hosted on Netlify. Each finished site loads quickly, costs nothing to host and has no plugins or page builder to maintain.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → landscaper-templates.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
Template 1, top to bottom. Templates 2 and 3 cover the same ground in a different order and style.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[0].title`
*Short name of one section of the page being walked through.*

```
Top bar, header and hero
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A thin bar with the phone, email and city, then the logo, six links and a quote button. The hero is one photo, one headline, two buttons and a call-today box, so the number is on screen before anyone scrolls.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[1].title`
*Short name of one section of the page being walked through.*

```
About, with three numbers
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A photo, two short paragraphs and three numbers (years in business, projects completed, local and family owned). The numbers are placeholders the owner replaces with their own.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[2].title`
*Short name of one section of the page being walked through.*

```
Four services
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Landscape design, installation, lawn maintenance and hardscapes as four photo cards, each with one line of text. The owner's real services and photos go here.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[3].title`
*Short name of one section of the page being walked through.*

```
One team, start to finish
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three photos, then six things the company handles, each with an icon and one sentence. This is where an owner lists what they actually do.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[4].title`
*Short name of one section of the page being walked through.*

```
Banner and seasonal tips
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A green banner with the quote button, then four cards, one per season, with what the yard needs. The cards give the site something to say all year and give search engines something to rank.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[5].title`
*Short name of one section of the page being walked through.*

```
Portfolio
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Four project photos in a row. The owner sends their own and I swap them in.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[6].title`
*Short name of one section of the page being walked through.*

```
Reviews
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three short quotes on a cream background. Placeholders until the owner sends their real reviews.
```

#### sections[7].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[7].title`
*Short name of one section of the page being walked through.*

```
Request a free quote
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Phone, email, address and hours on the left, a short quote form on the right: name, phone, email, service, message. In the template the form is a placeholder; it is connected to the owner's email when the site is customized.
```

#### sections[8].title
`src/data/project-walkthroughs.ts → landscaper-templates.sections[8].title`
*Short name of one section of the page being walked through.*

```
Call strip and footer
```

#### sections[8].caption
`src/data/project-walkthroughs.ts → landscaper-templates.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The phone number one more time in a dark strip, then a footer with quick links, services and contact details.
```

#### galleries[0].title
`src/data/project-walkthroughs.ts → landscaper-templates.galleries[0].title`
*Heading of an image grid.*

```
The three templates
```

#### galleries[0].intro
`src/data/project-walkthroughs.ts → landscaper-templates.galleries[0].intro`
*Intro line of an image grid.*

```
Three looks at the same business. Template 1 is classic with a serif headline, template 2 is bold with a boxed uppercase headline over a lawn photo, template 3 is centered and airy in green and cream.
```

#### galleries[0].items[0].caption
`src/data/project-walkthroughs.ts → landscaper-templates.galleries[0].items[0].caption`
*Caption under one image in a grid.*

```
Template 1: classic.
```

#### galleries[0].items[1].caption
`src/data/project-walkthroughs.ts → landscaper-templates.galleries[0].items[1].caption`
*Caption under one image in a grid.*

```
Template 2: bold.
```

#### galleries[0].items[2].caption
`src/data/project-walkthroughs.ts → landscaper-templates.galleries[0].items[2].caption`
*Caption under one image in a grid.*

```
Template 3: centered.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → landscaper-templates.phonesIntro`
*One line introducing the phone screenshots.*

```
The templates at phone width. One column, photos first, and a quote button on every screen.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → landscaper-templates.phones[0].caption`
*Caption under one phone screenshot.*

```
Template 1. The photo fills the first screen, with the headline and the quote button over it.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → landscaper-templates.phones[1].caption`
*Caption under one phone screenshot.*

```
The service cards become one column, each photo full width.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → landscaper-templates.phones[2].caption`
*Caption under one phone screenshot.*

```
Phone, email, address and hours come before the form, so calling is always the easier option.
```

#### phones[3].caption
`src/data/project-walkthroughs.ts → landscaper-templates.phones[3].caption`
*Caption under one phone screenshot.*

```
Template 2 on the same screen size. Same job, different look.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → landscaper-templates.details.design[0]`
*Bullet in the Design notes list.*

```
Three designs over the same content, so the owner compares layouts instead of imagining them.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → landscaper-templates.details.design[1]`
*Bullet in the Design notes list.*

```
On a desktop the phone number sits in the top bar and in the hero. On every screen size it comes back in the call strip above the footer.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → landscaper-templates.details.design[2]`
*Bullet in the Design notes list.*

```
Photos do the selling. The text is short and stays out of the way.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → landscaper-templates.details.design[3]`
*Bullet in the Design notes list.*

```
Generic on purpose: no fake company story, just the structure an owner can picture themselves in.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → landscaper-templates.details.build[0]`
*Bullet in the Build notes list.*

```
Hand-coded HTML, CSS and JavaScript. No framework, no page builder, no plugins.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → landscaper-templates.details.build[1]`
*Bullet in the Build notes list.*

```
The switcher is a fixed bar linking the three pages, so any template is one tap away at any point in the page.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → landscaper-templates.details.build[2]`
*Bullet in the Build notes list.*

```
Sections fade in as they scroll into view, with a small IntersectionObserver script. The whole page is under 50 KB of HTML.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → landscaper-templates.details.build[3]`
*Bullet in the Build notes list.*

```
Hosted free on Netlify. A finished client site costs nothing to run.
```

---

### Jonathan Zalzman Guitar

**Page:** `/web-developer-project-jonathan-zalzman`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Case study · client: Jonathan Zalzman (Suwanee, Georgia, US) · kind: Design and development · role: Design and development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Netlify Forms, Local business  
**Tools:** HTML & CSS, JavaScript, Netlify, Netlify Forms, Figma, Photoshop  

#### summary
`src/data/project-walkthroughs.ts → jonathan-zalzman.summary`
*NOT RENDERED ANYWHERE. The homepage card for this project comes from projects.ts instead. This field exists only because landing pages and case studies share one TypeScript type, and landing pages do render it. Do not spend effort rewriting it.*

```
Lesson booking site for a working musician in Suwanee, Georgia: nine teaching styles, transparent pricing, and a free trial lesson button that follows you down the page.
```

#### description
`src/data/project-walkthroughs.ts → jonathan-zalzman.description`
*One or two sentences directly under the page title.*

```
A website for a guitar teacher and working musician in Suwanee, Georgia. The goal of the site is to get visitors to book a free trial lesson.
```

#### pageDescription
`src/data/project-walkthroughs.ts → jonathan-zalzman.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
Jonathan Zalzman Guitar case study: the lesson booking site section by section and on a phone, and how the design and the booking form were built.
```

#### heroCaption
`src/data/project-walkthroughs.ts → jonathan-zalzman.heroCaption`
*Caption under the first screenshot.*

```
The first screen at 1680 pixels wide.
```

#### highlights[0]
`src/data/project-walkthroughs.ts → jonathan-zalzman.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Hand-coded — HTML, CSS and JavaScript, no page builder or plugins
```

#### highlights[1]
`src/data/project-walkthroughs.ts → jonathan-zalzman.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Two audiences — Adult players and parents, each given their own section on one page
```

#### highlights[2]
`src/data/project-walkthroughs.ts → jonathan-zalzman.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One booking form — Every call to action on the page leads to it
```

#### overviewTitle
`src/data/project-walkthroughs.ts → jonathan-zalzman.overviewTitle`
*Heading above the overview paragraphs.*

```
What the site has to do
```

#### overview[0]
`src/data/project-walkthroughs.ts → jonathan-zalzman.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Jonathan teaches guitar, bass and ukulele in Suwanee, Georgia and online, and he performs regularly. The site is built around that. The headline is "Learn guitar from a working musician", and the design and copy are meant to make that clear right away and then make booking a lesson easy.
```

#### overview[1]
`src/data/project-walkthroughs.ts → jonathan-zalzman.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The design uses performance photos on a dark background so the site looks like a musician's site rather than a directory listing. The nine styles he teaches (rock and blues, funk, jazz and fusion, prog and shred, metal, theory and ear training, bass and ukulele, songwriting, improvisation) each have a short description. Three five-star reviews from students of different ages are placed where a parent or an adult beginner would be deciding.
```

#### overview[2]
`src/data/project-walkthroughs.ts → jonathan-zalzman.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Pricing is shown on the page: the first lesson is free, then 30 or 60 minute lessons at a flat rate, with no registration fee, contract or subscription. The booking form asks for the instrument, experience level and whether the lessons are in person or online. Every call to action on the page links to the form, and his phone number and email are also shown.
```

#### overview[3]
`src/data/project-walkthroughs.ts → jonathan-zalzman.overview[3]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The site is hand-coded HTML, CSS and JavaScript hosted on Netlify. There is no page builder and there are no plugins to update.
```

#### walkthroughIntro
`src/data/project-walkthroughs.ts → jonathan-zalzman.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A condensed headline in orange and white, a stage photo, and a card that lists what students are learning right now. The two buttons are the only two things a visitor can do: book a free trial or watch him play.
```

#### sections[1].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[1].title`
*Short name of one section of the page being walked through.*

```
Ticker and bio
```

#### sections[1].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A scrolling ticker with the bands and the numbers (10+ years on Atlanta stages, 3 albums on Spotify), then a light section with his story in his words, a photo and a credentials card. The light background gives the eye a rest after the dark hero.
```

#### sections[2].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[2].title`
*Short name of one section of the page being walked through.*

```
Nine styles
```

#### sections[2].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A three-by-three grid, each style in its own color with two lines of copy. Under it, a bar for the visitor whose style is not listed, with the trial button again.
```

#### sections[3].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[3].title`
*Short name of one section of the page being walked through.*

```
Quote and three steps
```

#### sections[3].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A full-width stage photo with one quote over it, then how lessons start: free trial lesson, build a plan, show up and play. Numbered 01, 02, 03.
```

#### sections[4].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[4].title`
*Short name of one section of the page being walked through.*

```
For parents
```

#### sections[4].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three short answers for a parent deciding for a kid: a gentle start, progress you can see, somewhere to play. Parents are half the audience, so they get their own section.
```

#### sections[5].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[5].title`
*Short name of one section of the page being walked through.*

```
Pricing
```

#### sections[5].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three cards: the free first lesson, 30 minutes and 60 minutes, each with what is included and a button. No registration fee, no contract, no subscription is stated above the cards.
```

#### sections[6].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[6].title`
*Short name of one section of the page being walked through.*

```
Student reviews
```

#### sections[6].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three five-star reviews from students of different ages, each with who they are (a parent, an adult beginner, a teenager).
```

#### sections[7].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[7].title`
*Short name of one section of the page being walked through.*

```
I play guitar for a living
```

#### sections[7].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A band photo and three links: Spotify, YouTube and his band Free Hat. It lets a visitor hear him play before booking a lesson.
```

#### sections[8].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[8].title`
*Short name of one section of the page being walked through.*

```
Common questions
```

#### sections[8].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Seven questions in an accordion: is my kid too young, I have not played in 20 years, do I need my own guitar, what happens if I miss a lesson. These are the questions that otherwise arrive one at a time by email.
```

#### sections[9].title
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[9].title`
*Short name of one section of the page being walked through.*

```
Booking form and footer
```

#### sections[9].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.sections[9].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The form the whole page points at. It asks for the instrument, the student age, the experience level, in person or online, the plan and the best times, so the first reply can already suggest a slot. Under it, an email link and a phone number for people who would rather reach him directly.
```

#### phonesIntro
`src/data/project-walkthroughs.ts → jonathan-zalzman.phonesIntro`
*One line introducing the phone screenshots.*

```
The site on a phone, where most parents and students will find it.
```

#### phones[0].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.phones[0].caption`
*Caption under one phone screenshot.*

```
The headline still fills the screen, and the fixed bar keeps Book free trial and Call one tap away.
```

#### phones[1].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.phones[1].caption`
*Caption under one phone screenshot.*

```
The three-by-three grid of styles becomes one column, each style keeping its own color.
```

#### phones[2].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.phones[2].caption`
*Caption under one phone screenshot.*

```
Pricing stacks, and the free first lesson comes first so the risk is gone before the prices appear.
```

#### phones[3].caption
`src/data/project-walkthroughs.ts → jonathan-zalzman.phones[3].caption`
*Caption under one phone screenshot.*

```
The booking form at phone width, one field per line, with the bar still sitting at the bottom.
```

#### details.design[0]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.design[0]`
*Bullet in the Design notes list.*

```
Dark background, orange accent and condensed display type, so the site looks like a musician's site, not a tutoring directory.
```

#### details.design[1]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.design[1]`
*Bullet in the Design notes list.*

```
His own performance photos in every section. No stock photos.
```

#### details.design[2]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.design[2]`
*Bullet in the Design notes list.*

```
Proof sits between the price and the form: student reviews, then the band section, then the questions people actually ask.
```

#### details.design[3]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.design[3]`
*Bullet in the Design notes list.*

```
The trial lesson button is in the header, the hero, after the styles, after pricing and at the end. On phones it sits in a bar at the bottom of the screen.
```

#### details.build[0]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.build[0]`
*Bullet in the Build notes list.*

```
Hand-coded HTML, CSS and JavaScript. No framework, no page builder, no plugins.
```

#### details.build[1]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.build[1]`
*Bullet in the Build notes list.*

```
The credentials ticker, the pulsing trial button and the rise-in on the hero are CSS animations. The whole page is about 75 KB of HTML.
```

#### details.build[2]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.build[2]`
*Bullet in the Build notes list.*

```
The booking form runs on Netlify Forms with a honeypot field. It asks for the student's age, instrument, experience, in-person or online, the plan, best times and goals.
```

#### details.build[3]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.build[3]`
*Bullet in the Build notes list.*

```
On phones, a fixed bar at the bottom keeps Book free trial and Call one tap away the whole way down.
```

#### details.build[4]
`src/data/project-walkthroughs.ts → jonathan-zalzman.details.build[4]`
*Bullet in the Build notes list.*

```
Hosted free on Netlify.
```

---

### Explainer Visuals

**Page:** `/web-developer-project-explainer-visuals`  
**Note:** this case study is a standalone Astro page, not an entry in the data file.  
**Fixed labels (not prose, do not rewrite):** client: Explainer Visuals (video production studio) · role: Design and development · year: 2024  
**Tools:** Astro, HTML & CSS, JavaScript, Shell scripts, HLS video, Figma  

#### title
`src/pages/web-developer-project-explainer-visuals.astro → title`
*Page title, shown as the main heading.*

```
Explainer Visuals
```

#### description
`src/pages/web-developer-project-explainer-visuals.astro → description`
*One or two sentences directly under the page title.*

```
I built a portfolio website for a video production studio. Every video on the page plays automatically, so the site had to stay fast under that load.
```

#### slides[0].title
`src/pages/web-developer-project-explainer-visuals.astro → slides[0].title`
*Short label above one screenshot or video.*

```
Portfolio section
```

#### slides[1].title
`src/pages/web-developer-project-explainer-visuals.astro → slides[1].title`
*Short label above one screenshot or video.*

```
Process section
```

#### slides[2].title
`src/pages/web-developer-project-explainer-visuals.astro → slides[2].title`
*Short label above one screenshot or video.*

```
FAQ and footer
```

#### highlights[0]
`src/pages/web-developer-project-explainer-visuals.astro → highlights[0]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Every portfolio video plays at once without buffering
```

#### highlights[1]
`src/pages/web-developer-project-explainer-visuals.astro → highlights[1]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
HLS playlists
```

#### highlights[2]
`src/pages/web-developer-project-explainer-visuals.astro → highlights[2]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
MP4 files converted to streaming segments with a script I wrote
```

#### highlights[3]
`src/pages/web-developer-project-explainer-visuals.astro → highlights[3]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Static Astro site
```

#### highlights[4]
`src/pages/web-developer-project-explainer-visuals.astro → highlights[4]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
No server and no CMS
```

#### overview[0]
`src/pages/web-developer-project-explainer-visuals.astro → overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Explainer Visuals makes animated explainer videos for SaaS companies. Their portfolio is how they sell, and they wanted every video to play automatically as soon as the page opens. That is a lot of video for one page, so the main technical requirement was that the videos play without buffering.
```

#### overview[1]
`src/pages/web-developer-project-explainer-visuals.astro → overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
I started in Figma with their reference sites and made mockups the team could review before any code was written. The design is modern with some animation, and the site is built on Astro as a static site.
```

#### overview[2]
`src/pages/web-developer-project-explainer-visuals.astro → overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
To handle the video, I wrote a shell script that converts each MP4 into an HLS playlist, so each video streams in small segments instead of downloading as one file. The page loads quickly even with all the videos playing, and because the site is static there is nothing to update or renew.
```

---

### LoopManager

**Page:** `/web-developer-project-loopmanager`  
**Note:** this case study is a standalone Astro page, not an entry in the data file.  
**Fixed labels (not prose, do not rewrite):** client: nuVector LLC · role: Design, development and custom CMS · year: 2024  
**Tools:** Astro, HTML & CSS, JavaScript, WordPress (migrated from), HLS video, Figma, Photoshop, Canva  

#### title
`src/pages/web-developer-project-loopmanager.astro → title`
*Page title, shown as the main heading.*

```
LoopManager
```

#### description
`src/pages/web-developer-project-loopmanager.astro → description`
*One or two sentences directly under the page title.*

```
I redesigned the website for nuVector's LoopManager software. I moved it from WordPress to Astro and built a custom blog CMS so the marketing team could publish without a developer.
```

#### slides[0].title
`src/pages/web-developer-project-loopmanager.astro → slides[0].title`
*Short label above one screenshot or video.*

```
Product introduction
```

#### slides[1].title
`src/pages/web-developer-project-loopmanager.astro → slides[1].title`
*Short label above one screenshot or video.*

```
Product value
```

#### highlights[0]
`src/pages/web-developer-project-loopmanager.astro → highlights[0]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
WordPress to Astro
```

#### highlights[1]
`src/pages/web-developer-project-loopmanager.astro → highlights[1]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Faster pages and fewer moving parts
```

#### highlights[2]
`src/pages/web-developer-project-loopmanager.astro → highlights[2]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Custom CMS
```

#### highlights[3]
`src/pages/web-developer-project-loopmanager.astro → highlights[3]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
The marketing team publishes blog posts without a developer
```

#### highlights[4]
`src/pages/web-developer-project-loopmanager.astro → highlights[4]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Matched branding
```

#### highlights[5]
`src/pages/web-developer-project-loopmanager.astro → highlights[5]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Designed to match the new explainer video
```

#### overview[0]
`src/pages/web-developer-project-loopmanager.astro → overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
nuVector makes returnable asset management software, and LoopManager is their main product. They had just produced an animated explainer video and needed the website to match it, with consistent branding and a design that could show the video without slowing the site down.
```

#### overview[1]
`src/pages/web-developer-project-loopmanager.astro → overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
We designed the site in Figma and used its commenting features so decision makers could give feedback on the mockups as they were made. For the build, I recommended moving from WordPress to Astro. Static pages made the site faster, and I converted the videos from MP4 to HLS playlists with a custom script so they stream instead of downloading.
```

#### overview[2]
`src/pages/web-developer-project-loopmanager.astro → overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The marketing plan included a new blog post every two months, so I built a small custom content management system on top of the Astro site. The marketing director writes and publishes posts herself, without a developer.
```

---

### nuVector

**Page:** `/web-developer-project-nuvector`  
**Note:** this case study is a standalone Astro page, not an entry in the data file.  
**Fixed labels (not prose, do not rewrite):** client: nuVector LLC (Atlanta, Georgia) · role: Design and front-end development · year: 2024 to 2025  
**Tools:** Astro, HTML & CSS, JavaScript, TypeScript, Figma, SEO  

#### title
`src/pages/web-developer-project-nuvector.astro → title`
*Page title, shown as the main heading.*

```
nuVector
```

#### description
`src/pages/web-developer-project-nuvector.astro → description`
*One or two sentences directly under the page title.*

```
I redesigned and rebuilt nuVector's company website on Astro, with services, client logos, case studies and a blog. The main call to action is a demo request.
```

#### slides[0].title
`src/pages/web-developer-project-nuvector.astro → slides[0].title`
*Short label above one screenshot or video.*

```
Hero, services and clients
```

#### slides[1].title
`src/pages/web-developer-project-nuvector.astro → slides[1].title`
*Short label above one screenshot or video.*

```
Platform, case studies and about
```

#### slides[2].title
`src/pages/web-developer-project-nuvector.astro → slides[2].title`
*Short label above one screenshot or video.*

```
Blog and demo request
```

#### highlights[0]
`src/pages/web-developer-project-nuvector.astro → highlights[0]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Built on Astro
```

#### highlights[1]
`src/pages/web-developer-project-nuvector.astro → highlights[1]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Static pages with no plugins to maintain
```

#### highlights[2]
`src/pages/web-developer-project-nuvector.astro → highlights[2]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
SEO blog
```

#### highlights[3]
`src/pages/web-developer-project-nuvector.astro → highlights[3]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Posts written for specific searches, each ending at the demo form
```

#### highlights[4]
`src/pages/web-developer-project-nuvector.astro → highlights[4]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
One call to action
```

#### highlights[5]
`src/pages/web-developer-project-nuvector.astro → highlights[5]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Request a demo, repeated through the page
```

#### overview[0]
`src/pages/web-developer-project-nuvector.astro → overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The page opens with a short headline and a demo button. Below that are a services section (supply chain optimization, workflow tracking and data infrastructure), client logos including Volvo, Warner Bros. Discovery, Sonoco, Regal and ZF, a feature section, case studies, a short company history and the blog. The page ends with a full-width demo request section and a footer with contact details. The orange and charcoal brand colors and the nuVector logo mark are used throughout.
```

#### overview[1]
`src/pages/web-developer-project-nuvector.astro → overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The site is built on Astro as a static site, so pages load quickly and there is no plugin stack to maintain. The blog holds the SEO content, with posts on RFID readers, construction asset tracking and returnable asset trends. Each post targets a specific search and ends with a link to the demo form.
```

#### overview[2]
`src/pages/web-developer-project-nuvector.astro → overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Together with the LoopManager product site, this gave nuVector a consistent set of websites from company overview to product to demo request.
```

---

### Panda's Playhouse

**Page:** `/web-developer-project-panda`  
**Note:** this case study is a standalone Astro page, not an entry in the data file.  
**Fixed labels (not prose, do not rewrite):** client: Panda's Playhouse · role: UX design and Wix Studio development · year: 2023  
**Tools:** Wix Studio, JavaScript (Velo), Third-party APIs, CSS, Figma  

#### title
`src/pages/web-developer-project-panda.astro → title`
*Page title, shown as the main heading.*

```
Panda's Playhouse
```

#### description
`src/pages/web-developer-project-panda.astro → description`
*One or two sentences directly under the page title.*

```
A membership website for an online gaming community that had outgrown its Facebook page, built on Wix Studio with Velo so the owners' team could maintain it themselves.
```

#### slides[0].title
`src/pages/web-developer-project-panda.astro → slides[0].title`
*Short label above one screenshot or video.*

```
Step 1: Figma design
```

#### slides[1].title
`src/pages/web-developer-project-panda.astro → slides[1].title`
*Short label above one screenshot or video.*

```
High-fidelity Figma design of the Panda's Playhouse homepage
```

#### slides[2].title
`src/pages/web-developer-project-panda.astro → slides[2].title`
*Short label above one screenshot or video.*

```
Step 2: Prototype
```

#### slides[3].title
`src/pages/web-developer-project-panda.astro → slides[3].title`
*Short label above one screenshot or video.*

```
Step 3: Development
```

#### highlights[0]
`src/pages/web-developer-project-panda.astro → highlights[0]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
5,000+ members
```

#### highlights[1]
`src/pages/web-developer-project-panda.astro → highlights[1]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Moved from Facebook to their own website
```

#### highlights[2]
`src/pages/web-developer-project-panda.astro → highlights[2]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
ID verification
```

#### highlights[3]
`src/pages/web-developer-project-panda.astro → highlights[3]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Facial verification for new sign-ups through a third-party API
```

#### highlights[4]
`src/pages/web-developer-project-panda.astro → highlights[4]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Members area
```

#### highlights[5]
`src/pages/web-developer-project-panda.astro → highlights[5]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
A members-only section and a virtual currency system
```

#### overview[0]
`src/pages/web-developer-project-panda.astro → overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Panda's Playhouse had grown to more than 5,000 active members through Facebook ads and needed a proper membership website. The requirements were identity verification for new members, a members-only area, a way to manage their virtual currency and a site that worked well on phones. The owners wanted it built on Wix so their virtual assistants could update it without hiring a developer.
```

#### overview[1]
`src/pages/web-developer-project-panda.astro → overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
We started with low-fidelity wireframes to work out the flow from the landing page to a signed-in member, then moved to high-fidelity designs and a clickable prototype in Figma. Development started after the flow and colors were approved.
```

#### overview[2]
`src/pages/web-developer-project-panda.astro → overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
I used Wix Studio's Velo layer to build the custom parts: a facial verification step using a third-party API, a members-only area, and the virtual currency system used inside the community. The site is responsive and works across browsers.
```

---

### MMA Class Tracker

**Page:** `/web-developer-project-3`  
**Note:** this case study is a standalone Astro page, not an entry in the data file.  
**Fixed labels (not prose, do not rewrite):** client: Side project · role: Design and development · year: 2023  
**Tools:** HTML & CSS, JavaScript, Google Firebase, GitHub, Netlify  

#### title
`src/pages/web-developer-project-3.astro → title`
*Page title, shown as the main heading.*

```
MMA Class Tracker
```

#### pageTitle
`src/pages/web-developer-project-3.astro → pageTitle`
*Browser tab title and search result headline.*

```
MMA Class Tracker side project | Manny Pachano
```

#### description
`src/pages/web-developer-project-3.astro → description`
*One or two sentences directly under the page title.*

```
A small web app that tracks martial arts class attendance with experience points and levels. Progress syncs across devices.
```

#### slides[0].title
`src/pages/web-developer-project-3.astro → slides[0].title`
*Short label above one screenshot or video.*

```
The app
```

#### highlights[0]
`src/pages/web-developer-project-3.astro → highlights[0]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Points per class
```

#### highlights[1]
`src/pages/web-developer-project-3.astro → highlights[1]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Harder classes are worth more points
```

#### highlights[2]
`src/pages/web-developer-project-3.astro → highlights[2]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Progress bars and a new character at each level
```

#### highlights[3]
`src/pages/web-developer-project-3.astro → highlights[3]`
*Stat tile under the first screen. These come in value and label pairs, in order.*

```
Progress is stored in Firebase and works on any device
```

#### overview[0]
`src/pages/web-developer-project-3.astro → overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
My friends and I wanted a reason to keep going to martial arts class, so I built a tracker that turns attendance into a game. Each class you attend adds experience points, harder classes add more, and when the bar fills you level up and unlock a new character.
```

#### overview[1]
`src/pages/web-developer-project-3.astro → overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The front end is HTML, CSS and JavaScript with animated progress bars. Google Firebase stores everyone's progress so the app stays in sync between phones and laptops. It is fully responsive, which was one of the reasons I built it. I wanted practice with responsive layouts.
```

#### overview[2]
`src/pages/web-developer-project-3.astro → overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
It is a small app, but it worked. We went to class more often, and the project gave me practice with state, syncing and animation that I still use on client work.
```

---

## 4. Landing page walkthroughs

### Rythm

**Page:** `/landing-page-rythm`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Rythm Health (concept, not commissioned) · kind: Concept redesign · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Responsive layout, Light and dark themes, CSS animation, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → rythm.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A concept redesign of the homepage for Rythm Health, an at-home blood testing company in the US. One HTML file with hand-written CSS and JavaScript: an animated hero, numbers that count up, a scrolling wall of reviews, a working FAQ, and <strong>a light and a dark version you can switch between.</strong>
```

#### description
`src/data/landing-pages.ts → rythm.description`
*One or two sentences directly under the page title.*

```
A concept redesign of the homepage for Rythm Health, an at-home blood testing company. I designed and built it in September 2026 to show how I would present their product: one page, one clear offer, and a light and a dark version. It is not a client project and I am not affiliated with Rythm.
```

#### pageDescription
`src/data/landing-pages.ts → rythm.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
A concept redesign of the Rythm Health homepage, shown section by section: an animated hero, count-up numbers, a review marquee, a working FAQ and light and dark versions.
```

#### facts[0]
`src/data/landing-pages.ts → rythm.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site, light and dark
```

#### compare.labelA
`src/data/landing-pages.ts → rythm.compare.labelA`
*Label on the left side of the before/after slider.*

```
Light
```

#### compare.labelB
`src/data/landing-pages.ts → rythm.compare.labelB`
*Label on the right side of the before/after slider.*

```
Dark
```

#### heroCaption
`src/data/landing-pages.ts → rythm.heroCaption`
*Caption under the first screenshot.*

```
The first screen in both versions. Drag the handle to compare them. On the live page a button in the header switches the theme.
```

#### overviewTitle
`src/data/landing-pages.ts → rythm.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page has to do
```

#### overview[0]
`src/data/landing-pages.ts → rythm.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Rythm sells a monthly at-home blood test. The page has to explain the product to someone who has never heard of it, give them reasons to trust it, and get them to press one button: <strong>Get started</strong>. Everything on the page is there for one of those three jobs.
```

#### overview[1]
`src/data/landing-pages.ts → rythm.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The order of the sections follows how people decide. First the offer and the price, then proof from people they recognize, then how the test works, then the numbers, then the product card. After that comes the detail for people who keep reading: the science, the dashboard, the use cases, privacy, reviews and questions. The page ends with one last call to action. The same red button appears in the header, the hero, the product card and the end.
```

#### overview[2]
`src/data/landing-pages.ts → rythm.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The design uses two typefaces, Space Grotesk for headlines and Manrope for text, one accent color and a lot of space. One word in the main headline is red. The dark version is the default. The light version is the same layout with nine color variables swapped.
```

#### walkthroughIntro
`src/data/landing-pages.ts → rythm.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
Thirteen sections, top to bottom. Use the switch to see them in the other version.
```

#### sections[0].title
`src/data/landing-pages.ts → rythm.sections[0].title`
*Short name of one section of the page being walked through.*

```
Announcement bar, header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → rythm.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The offer is on screen before anyone scrolls: the headline with one word in red, the discounted price, two buttons and three trust points. The red sphere floats, three result badges float around it, and the text rises in one line at a time when the page loads.
```

#### sections[1].title
`src/data/landing-pages.ts → rythm.sections[1].title`
*Short name of one section of the page being walked through.*

```
Trust strip and quotes
```

#### sections[1].caption
`src/data/landing-pages.ts → rythm.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A strip of certifications scrolls by on a loop (HIPAA, FDA, CLIA, CAP). Below it, six short quotes from athletes, founders and a surgeon, each with initials in a colored circle instead of a photo.
```

#### sections[2].title
`src/data/landing-pages.ts → rythm.sections[2].title`
*Short name of one section of the page being walked through.*

```
How it works, and the numbers
```

#### sections[2].caption
`src/data/landing-pages.ts → rythm.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three numbered steps in one row, then four percentages. The numbers count up from zero the first time they scroll into view, with an IntersectionObserver and requestAnimationFrame.
```

#### sections[3].title
`src/data/landing-pages.ts → rythm.sections[3].title`
*Short name of one section of the page being walked through.*

```
Biomarkers
```

#### sections[3].caption
`src/data/landing-pages.ts → rythm.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The text says what the test measures. The grid lists markers with Optimal or Out of range pills, green and amber, so the reader gets the idea of the product without reading a paragraph.
```

#### sections[4].title
`src/data/landing-pages.ts → rythm.sections[4].title`
*Short name of one section of the page being walked through.*

```
Membership card
```

#### sections[4].caption
`src/data/landing-pages.ts → rythm.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One product, one card. The original price crossed out, the discount badge, six included items with check marks, the button, and the guarantee lines underneath, where people look for the catch.
```

#### sections[5].title
`src/data/landing-pages.ts → rythm.sections[5].title`
*Short name of one section of the page being walked through.*

```
Less blood, and the dashboard
```

#### sections[5].caption
`src/data/landing-pages.ts → rythm.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Two bars drawn with CSS compare about 10 ml to about 1 ml. Below that, a mock dashboard with a health score ring, a trend line and an insight card, all built with HTML, CSS and inline SVG. No screenshots.
```

#### sections[6].title
`src/data/landing-pages.ts → rythm.sections[6].title`
*Short name of one section of the page being walked through.*

```
The lab, and real results
```

#### sections[6].caption
`src/data/landing-pages.ts → rythm.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A photo next to a short statement, then three plain cards. It breaks up the product sections with something human before the page gets into detail again.
```

#### sections[7].title
`src/data/landing-pages.ts → rythm.sections[7].title`
*Short name of one section of the page being walked through.*

```
Six use cases
```

#### sections[7].caption
`src/data/landing-pages.ts → rythm.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One grid, six cards, one icon each. It is the same card component as the steps and the security section, so the page stays consistent and the CSS stays short.
```

#### sections[8].title
`src/data/landing-pages.ts → rythm.sections[8].title`
*Short name of one section of the page being walked through.*

```
Privacy and security
```

#### sections[8].caption
`src/data/landing-pages.ts → rythm.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three cards with green icons. Health data is the obvious objection for this product, so it gets its own section before the reviews.
```

#### sections[9].title
`src/data/landing-pages.ts → rythm.sections[9].title`
*Short name of one section of the page being walked through.*

```
Reviews
```

#### sections[9].caption
`src/data/landing-pages.ts → rythm.sections[9].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Two rows of review cards scroll in opposite directions and pause when you hover. Each row is duplicated once in JavaScript so the loop never shows a gap.
```

#### sections[10].title
`src/data/landing-pages.ts → rythm.sections[10].title`
*Short name of one section of the page being walked through.*

```
Community
```

#### sections[10].caption
`src/data/landing-pages.ts → rythm.sections[10].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A photo grid for the social feed with tiles in the brand red. The tiles scale up slightly on hover.
```

#### sections[11].title
`src/data/landing-pages.ts → rythm.sections[11].title`
*Short name of one section of the page being walked through.*

```
FAQ
```

#### sections[11].caption
`src/data/landing-pages.ts → rythm.sections[11].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Seven questions in an accordion. One answer is open at a time and the height animates, so the page never jumps.
```

#### sections[12].title
`src/data/landing-pages.ts → rythm.sections[12].title`
*Short name of one section of the page being walked through.*

```
Final call to action and footer
```

#### sections[12].caption
`src/data/landing-pages.ts → rythm.sections[12].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The brand line, one sentence, one button, then a four-column footer with the legal text. The last line credits the concept to me.
```

#### phonesIntro
`src/data/landing-pages.ts → rythm.phonesIntro`
*One line introducing the phone screenshots.*

```
Below 920 pixels the page goes to one column, the header menu becomes a full-screen overlay and the buttons go full width. These are four screens from the light version at 480 pixels wide.
```

#### phones[0].caption
`src/data/landing-pages.ts → rythm.phones[0].caption`
*Caption under one phone screenshot.*

```
The hero: the sphere first, then the headline, the price and the buttons.
```

#### phones[1].caption
`src/data/landing-pages.ts → rythm.phones[1].caption`
*Caption under one phone screenshot.*

```
How it works: the three steps stack, then the numbers.
```

#### phones[2].caption
`src/data/landing-pages.ts → rythm.phones[2].caption`
*Caption under one phone screenshot.*

```
The membership card fills the width.
```

#### phones[3].caption
`src/data/landing-pages.ts → rythm.phones[3].caption`
*Caption under one phone screenshot.*

```
The FAQ and the final call to action.
```

#### details.design[0]
`src/data/landing-pages.ts → rythm.details.design[0]`
*Bullet in the Design notes list.*

```
Two typefaces: Space Grotesk for headlines, Manrope for text. Headlines run up to 83 pixels on desktop.
```

#### details.design[1]
`src/data/landing-pages.ts → rythm.details.design[1]`
*Bullet in the Design notes list.*

```
One accent color. Red is used for one word in the main headline, the buttons and the small labels above section titles.
```

#### details.design[2]
`src/data/landing-pages.ts → rythm.details.design[2]`
*Bullet in the Design notes list.*

```
Two themes from one set of CSS variables. The dark version is the default; the light version swaps nine variables.
```

#### details.design[3]
`src/data/landing-pages.ts → rythm.details.design[3]`
*Bullet in the Design notes list.*

```
Every section has one job, and the sections that matter most end in the same button.
```

#### details.design[4]
`src/data/landing-pages.ts → rythm.details.design[4]`
*Bullet in the Design notes list.*

```
Cards, pills and buttons share the same corner radius and border treatment, so the components stay consistent across the page.
```

#### details.build[0]
`src/data/landing-pages.ts → rythm.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 70 KB, with hand-written CSS and JavaScript. No framework and no build step.
```

#### details.build[1]
`src/data/landing-pages.ts → rythm.details.build[1]`
*Bullet in the Build notes list.*

```
All icons and graphics are inline SVG or plain CSS. The only image files are three stock photos.
```

#### details.build[2]
`src/data/landing-pages.ts → rythm.details.build[2]`
*Bullet in the Build notes list.*

```
Reveal on scroll, the count-up numbers and the review marquee run on IntersectionObserver and CSS animations, and all of it switches off for people who prefer reduced motion.
```

#### details.build[3]
`src/data/landing-pages.ts → rythm.details.build[3]`
*Bullet in the Build notes list.*

```
The theme toggle, the mobile menu, the accordion and the count-up add up to about 40 lines of JavaScript.
```

#### details.build[4]
`src/data/landing-pages.ts → rythm.details.build[4]`
*Bullet in the Build notes list.*

```
Two breakpoints, 920 and 600 pixels. Deployed on Netlify.
```

---

### Apple SEO

**Page:** `/landing-page-apple-seo`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Apple SEO, a local SEO agency in State College, Pennsylvania (unsolicited concept, not commissioned) · kind: Concept redesign · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Conversion design, Information hierarchy, SVG, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → apple-seo.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A redesign of the homepage for a local SEO agency, built without being asked. Their services, their case studies and their brand stayed. <strong>What changed is the order, one consistent format for showing proof, and three new sections that answer the questions a discovery call normally has to cover.</strong>
```

#### description
`src/data/landing-pages.ts → apple-seo.description`
*One or two sentences directly under the page title.*

```
A redesign of the Apple SEO homepage, designed and built in 2026. It was not commissioned and I am not affiliated with them. Their copy, services and case studies are theirs; the structure, the design and the code are mine. Press Show notes on the live page to see what changed in each section and why.
```

#### pageDescription
`src/data/landing-pages.ts → apple-seo.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
An unsolicited concept redesign of the Apple SEO homepage, shown section by section: a six-month timeline, three case studies in one consistent proof format, a combined results band, an FAQ and an audit request form.
```

#### facts[0]
`src/data/landing-pages.ts → apple-seo.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site
```

#### heroCaption
`src/data/landing-pages.ts → apple-seo.heroCaption`
*Caption under the first screenshot.*

```
The first screen. The panel on the right is a local search result drawn in HTML rather than screenshotted, showing the one thing the agency sells: a business at rank seven moving into the top three.
```

#### highlights[0]
`src/data/landing-pages.ts → apple-seo.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Answered before the call — A six-month timeline and an FAQ, so the questions that eat a discovery call are handled on the page
```

#### highlights[1]
`src/data/landing-pages.ts → apple-seo.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One proof format — All three case studies get the same four stat tiles, so the strongest one stops outshining the other two
```

#### highlights[2]
`src/data/landing-pages.ts → apple-seo.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One HTML file — About 56 KB, hand-written, no framework and no build step
```

#### overviewTitle
`src/data/landing-pages.ts → apple-seo.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page has to do
```

#### overview[0]
`src/data/landing-pages.ts → apple-seo.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Apple SEO already had the hard part: three real clients, three industries, and results they could name. What the homepage did not do was put them in the order a buyer thinks in. So the structure changed and almost none of the words did. The offer, then what you get, then what the first six months look like, then the proof, then the people, then the questions, then the form.
```

#### overview[1]
`src/data/landing-pages.ts → apple-seo.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Two fixes rather than additions. The three case studies were each styled differently, which made the strongest one look like the only one. They now share a single format, so the three read as a repeatable process. The page also ended at a booking tool, which loses anyone not ready to pick a time, so it now ends at a form that accepts an email instead.
```

#### overview[2]
`src/data/landing-pages.ts → apple-seo.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
This was not commissioned and I am not affiliated with Apple SEO. The copy, the services and the case study figures are theirs. The structure, the design, the code and the three new sections are mine. The live page keeps a Show notes button that explains each change in place, which is how it was sent to them.
```

#### walkthroughIntro
`src/data/landing-pages.ts → apple-seo.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/landing-pages.ts → apple-seo.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → apple-seo.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One sentence for what the agency does, with the word that matters in red. The panel beside it is a local search result drawn in HTML, showing a business at rank seven with the result above it highlighted. Under the fold line, four figures that hold up the claim.
```

#### sections[1].title
`src/data/landing-pages.ts → apple-seo.sections[1].title`
*Short name of one section of the page being walked through.*

```
What we do
```

#### sections[1].caption
`src/data/landing-pages.ts → apple-seo.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Six services on one card style with one icon each, rather than six different treatments. The line above them says most clients start with the profile and the ads, which turns a menu into a recommendation.
```

#### sections[2].title
`src/data/landing-pages.ts → apple-seo.sections[2].title`
*Short name of one section of the page being walked through.*

```
How it works
```

#### sections[2].caption
`src/data/landing-pages.ts → apple-seo.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section. The old page listed services but never said what the first month looks like, which is the question every prospect asks on the call. Four steps with the week or month each one lands in, and a line saying there is no contract before the audit.
```

#### sections[3].title
`src/data/landing-pages.ts → apple-seo.sections[3].title`
*Short name of one section of the page being walked through.*

```
Case study: junk removal
```

#### sections[3].caption
`src/data/landing-pages.ts → apple-seo.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The first of three, and the template for the other two. Their own client, their own figures. The layout gives it a paragraph of narrative, three checked specifics, the tactics as tags, four stat tiles and a photograph. The photo caption says it is a stock stand-in rather than pretending otherwise.
```

#### sections[4].title
`src/data/landing-pages.ts → apple-seo.sections[4].title`
*Short name of one section of the page being walked through.*

```
Case study: dermatology
```

#### sections[4].caption
`src/data/landing-pages.ts → apple-seo.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The same format applied to a different problem: cleaning up forty locations rather than starting from nothing. Two of the tiles show a zero, for no ad spend and no duplicate listings remaining.
```

#### sections[5].title
`src/data/landing-pages.ts → apple-seo.sections[5].title`
*Short name of one section of the page being walked through.*

```
Case study: roofing
```

#### sections[5].caption
`src/data/landing-pages.ts → apple-seo.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The third case study, at the largest scale: fifteen markets rather than one town. The three are ordered smallest to largest so the scale builds across the section.
```

#### sections[6].title
`src/data/landing-pages.ts → apple-seo.sections[6].title`
*Short name of one section of the page being walked through.*

```
Across every client
```

#### sections[6].caption
`src/data/landing-pages.ts → apple-seo.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section that totals the three case studies into four figures, so a visitor who skimmed them still gets the scale. The line underneath states that the figures come from accounts the agency runs, not from an industry average.
```

#### sections[7].title
`src/data/landing-pages.ts → apple-seo.sections[7].title`
*Short name of one section of the page being walked through.*

```
Who we are
```

#### sections[7].caption
`src/data/landing-pages.ts → apple-seo.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their positioning and their voice, set in two columns with enough contrast to actually read on a dark background. Three checked promises sit under it, and each one is about access rather than results: you talk to the person doing the work.
```

#### sections[8].title
`src/data/landing-pages.ts → apple-seo.sections[8].title`
*Short name of one section of the page being walked through.*

```
Before you call
```

#### sections[8].caption
`src/data/landing-pages.ts → apple-seo.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section. Six questions in an accordion, and the two that matter most are the uncomfortable ones: whether there is a long contract, and who owns the profiles and ad accounts afterwards. An agency that answers those on the page saves itself the defensive half of every first call.
```

#### sections[9].title
`src/data/landing-pages.ts → apple-seo.sections[9].title`
*Short name of one section of the page being walked through.*

```
Contact and footer
```

#### sections[9].caption
`src/data/landing-pages.ts → apple-seo.sections[9].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The old page ended at a button that opened a booking tool, which loses anyone not ready to pick a time on the spot. This one offers both: call during business hours, or fill in the form and get a reply the same business day. The form asks what they need first, so the reply can already be specific.
```

#### phonesIntro
`src/data/landing-pages.ts → apple-seo.phonesIntro`
*One line introducing the phone screenshots.*

```
The page on a phone, where most local search traffic arrives.
```

#### phones[0].caption
`src/data/landing-pages.ts → apple-seo.phones[0].caption`
*Caption under one phone screenshot.*

```
The search result panel moves below the buttons, so the claim is read before the illustration of it.
```

#### phones[1].caption
`src/data/landing-pages.ts → apple-seo.phones[1].caption`
*Caption under one phone screenshot.*

```
The timeline becomes vertical, with the numbered markers holding the eye down the column.
```

#### phones[2].caption
`src/data/landing-pages.ts → apple-seo.phones[2].caption`
*Caption under one phone screenshot.*

```
Case study tiles go one per row rather than shrinking, so the numbers stay legible.
```

#### phones[3].caption
`src/data/landing-pages.ts → apple-seo.phones[3].caption`
*Caption under one phone screenshot.*

```
Call and form sit one above the other, so whichever the visitor prefers is one tap away.
```

#### details.design[0]
`src/data/landing-pages.ts → apple-seo.details.design[0]`
*Bullet in the Design notes list.*

```
Their dark charcoal and red were kept. What changed is that red now marks one thing at a time rather than competing with yellow and green boxes.
```

#### details.design[1]
`src/data/landing-pages.ts → apple-seo.details.design[1]`
*Bullet in the Design notes list.*

```
The three case studies use one shared format. Styling them differently made the best one look like the only one.
```

#### details.design[2]
`src/data/landing-pages.ts → apple-seo.details.design[2]`
*Bullet in the Design notes list.*

```
Two typefaces: Poppins for headlines, Figtree for text. Figures get their own weight so a reader can scan the proof without reading the page.
```

#### details.design[3]
`src/data/landing-pages.ts → apple-seo.details.design[3]`
*Bullet in the Design notes list.*

```
The search result panel in the hero is drawn rather than screenshotted, so it stays sharp and cannot go out of date.
```

#### details.design[4]
`src/data/landing-pages.ts → apple-seo.details.design[4]`
*Bullet in the Design notes list.*

```
Every section that could lead to contact ends in the same red button, and the header carries one the whole way down.
```

#### details.build[0]
`src/data/landing-pages.ts → apple-seo.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 56 KB, with hand-written CSS and JavaScript. No framework and no build step.
```

#### details.build[1]
`src/data/landing-pages.ts → apple-seo.details.build[1]`
*Bullet in the Build notes list.*

```
Seven figures count up when their block comes into view, on IntersectionObserver, and hold their value for anyone who prefers reduced motion.
```

#### details.build[2]
`src/data/landing-pages.ts → apple-seo.details.build[2]`
*Bullet in the Build notes list.*

```
The FAQ uses native details elements, so it opens before the JavaScript runs.
```

#### details.build[3]
`src/data/landing-pages.ts → apple-seo.details.build[3]`
*Bullet in the Build notes list.*

```
Four breakpoints, 1000, 820, 600 and 520 pixels.
```

#### details.build[4]
`src/data/landing-pages.ts → apple-seo.details.build[4]`
*Bullet in the Build notes list.*

```
The annotation layer is part of the file: a Show notes button reveals a note on every section explaining what changed. It is off by default so the page loads as a normal page.
```

#### details.build[5]
`src/data/landing-pages.ts → apple-seo.details.build[5]`
*Bullet in the Build notes list.*

```
Deployed on Netlify.
```

---

### Rise Talent Advisors

**Page:** `/landing-page-rise-talent`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Rise Talent Advisors, a staffing and recruiting firm in Lehi, Utah (unsolicited concept, not commissioned) · kind: Concept redesign · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Conversion design, Two-audience layout, Responsive layout, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → rise-talent.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A redesign of the homepage for a staffing firm, built without being asked. Their brand, their client logos and their copy stayed. <strong>What changed is that the page now says what happens after you fill in the form, names the roles they actually place, and speaks to candidates as well as companies.</strong>
```

#### description
`src/data/landing-pages.ts → rise-talent.description`
*One or two sentences directly under the page title.*

```
A redesign of the Rise Talent Advisors homepage, designed and built in 2026. It was not commissioned and I am not affiliated with them. Their copy, brand and client logos are theirs; the structure, the design and the code are mine. Press Show notes on the live page to see what changed in each section and why.
```

#### pageDescription
`src/data/landing-pages.ts → rise-talent.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
An unsolicited concept redesign of the Rise Talent Advisors homepage, shown section by section: a four-step hiring process, the specific roles they place, a section for candidates, and a hire-request form in the hero.
```

#### facts[0]
`src/data/landing-pages.ts → rise-talent.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site
```

#### heroCaption
`src/data/landing-pages.ts → rise-talent.heroCaption`
*Caption under the first screenshot.*

```
The first screen. The form is in the hero rather than at the bottom, because a hiring manager who lands here already knows what they want. The three figures answer the only question they have before typing.
```

#### highlights[0]
`src/data/landing-pages.ts → rise-talent.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
What happens next — Four steps from intake call to signed offer, with the day or week each one lands in
```

#### highlights[1]
`src/data/landing-pages.ts → rise-talent.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Both audiences — Companies and candidates each get a section, instead of the page speaking only to companies
```

#### highlights[2]
`src/data/landing-pages.ts → rise-talent.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Specific roles — Twelve named job titles in place of five industry words, so a hiring manager can tell if they are the right call
```

#### overviewTitle
`src/data/landing-pages.ts → rise-talent.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page has to do
```

#### overview[0]
`src/data/landing-pages.ts → rise-talent.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
A staffing firm competes on trust more than on price, and trust here comes from specificity. The existing page said what Rise does and which industries they serve, but a hiring manager reading "IT, Fintech, Accounting, Construction and Operations" still cannot tell whether these people have placed their role before. So the redesign names twelve actual job titles, and puts a four-step process on the page saying exactly what happens after the form goes in.
```

#### overview[1]
`src/data/landing-pages.ts → rise-talent.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The second change is that the page now has two audiences. The original spoke only to companies, which means a candidate who lands on it has nowhere to go, and a staffing firm without candidates has nothing to sell. The candidate section sits low on the page, below everything a company needs, with a list of recently posted roles and a way to send a resume without applying to anything.
```

#### overview[2]
`src/data/landing-pages.ts → rise-talent.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
This was not commissioned and I am not affiliated with Rise. Their copy, their navy and orange, their gradient and their real client logos all stayed. The structure, the three new sections and the code are mine. The live page keeps a Show notes button that explains each change in place, which is how it was sent to them.
```

#### walkthroughIntro
`src/data/landing-pages.ts → rise-talent.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/landing-pages.ts → rise-talent.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → rise-talent.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The headline is theirs and it is the right one, so it stayed. The form sits beside it rather than at the bottom of the page, and asks four things, not ten. Underneath, the client logos in grayscale so they read as proof rather than competing with the orange.
```

#### sections[1].title
`src/data/landing-pages.ts → rise-talent.sections[1].title`
*Short name of one section of the page being walked through.*

```
Precision-driven talent advisory
```

#### sections[1].caption
`src/data/landing-pages.ts → rise-talent.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their positioning, their button, their copy. What changed is the image treatment: one photograph with a small floating card on it, instead of a collage. The four hiring models sit as checked items so a reader can find theirs in a second.
```

#### sections[2].title
`src/data/landing-pages.ts → rise-talent.sections[2].title`
*Short name of one section of the page being walked through.*

```
What sets our hiring model apart
```

#### sections[2].caption
`src/data/landing-pages.ts → rise-talent.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their three differentiators, each on a clean card with an orange icon tile. The copy is unchanged. The point of the card treatment is that three claims of equal weight should look equal, which they did not before.
```

#### sections[3].title
`src/data/landing-pages.ts → rise-talent.sections[3].title`
*Short name of one section of the page being walked through.*

```
How it works
```

#### sections[3].caption
`src/data/landing-pages.ts → rise-talent.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section, and the one that does the most work. The original page said what Rise does but never what happens after someone fills in the form, which is the reason people hesitate. Four steps, each with the day or week it lands in, ending with staying in touch through the first ninety days.
```

#### sections[4].title
`src/data/landing-pages.ts → rise-talent.sections[4].title`
*Short name of one section of the page being walked through.*

```
The numbers
```

#### sections[4].caption
`src/data/landing-pages.ts → rise-talent.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their navy band with orange numbers, kept in substance and tidied in spacing, with the figures counting up as the band comes into view. The original had a fourth stat reading "Days" with no number in front of it, which looks like an unfinished placeholder, so it is left out until they supply the figure.
```

#### sections[5].title
`src/data/landing-pages.ts → rise-talent.sections[5].title`
*Short name of one section of the page being walked through.*

```
Roles we fill
```

#### sections[5].caption
`src/data/landing-pages.ts → rise-talent.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section. Five practice areas as filters, and behind each one the actual job titles they place. Naming the titles tells a hiring manager whether the firm has filled their specific role, which a category label alone does not.
```

#### sections[6].title
`src/data/landing-pages.ts → rise-talent.sections[6].title`
*Short name of one section of the page being walked through.*

```
Industries
```

#### sections[6].caption
`src/data/landing-pages.ts → rise-talent.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The three links to their existing industry pages, kept. They are photo cards with the text set into the image rather than beside it, which holds the row together at every width.
```

#### sections[7].title
`src/data/landing-pages.ts → rise-talent.sections[7].title`
*Short name of one section of the page being walked through.*

```
For candidates
```

#### sections[7].caption
`src/data/landing-pages.ts → rise-talent.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A new section, and the reason it sits this low is deliberate: everything a company needs comes first. Recently posted roles with location, working model and pay range, plus a way to send a resume without applying to anything specific.
```

#### sections[8].title
`src/data/landing-pages.ts → rise-talent.sections[8].title`
*Short name of one section of the page being walked through.*

```
Closing and footer
```

#### sections[8].caption
`src/data/landing-pages.ts → rise-talent.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their navy to orange gradient and their copy, kept, because it is one of the strongest parts of the current site. It offers a fifteen-minute call and a phone number, and says out loud that they will tell you if they are not the right fit. The footer repeats the practice areas and the industries.
```

#### phonesIntro
`src/data/landing-pages.ts → rise-talent.phonesIntro`
*One line introducing the phone screenshots.*

```
The page on a phone. A hiring manager checking a recommendation is usually doing it between meetings.
```

#### phones[0].caption
`src/data/landing-pages.ts → rise-talent.phones[0].caption`
*Caption under one phone screenshot.*

```
The figures stack with the label beside each number, so the column stays short and the form stays close.
```

#### phones[1].caption
`src/data/landing-pages.ts → rise-talent.phones[1].caption`
*Caption under one phone screenshot.*

```
The process runs down the center, with the timeframes acting as the rungs between steps.
```

#### phones[2].caption
`src/data/landing-pages.ts → rise-talent.phones[2].caption`
*Caption under one phone screenshot.*

```
The practice area filters wrap rather than scroll, so every one is visible without swiping.
```

#### phones[3].caption
`src/data/landing-pages.ts → rise-talent.phones[3].caption`
*Caption under one phone screenshot.*

```
The open roles list keeps its pay ranges at phone width, which is the part candidates scroll for.
```

#### details.design[0]
`src/data/landing-pages.ts → rise-talent.details.design[0]`
*Bullet in the Design notes list.*

```
Their navy, their orange, the navy to orange gradient and the orange footer bar all stayed. The brand was not the problem.
```

#### details.design[1]
`src/data/landing-pages.ts → rise-talent.details.design[1]`
*Bullet in the Design notes list.*

```
Client logos are grayscale so they read as proof and stop competing with the accent color.
```

#### details.design[2]
`src/data/landing-pages.ts → rise-talent.details.design[2]`
*Bullet in the Design notes list.*

```
One typeface, Figtree, at four weights. A staffing page is mostly lists and figures, and one family keeps them aligned.
```

#### details.design[3]
`src/data/landing-pages.ts → rise-talent.details.design[3]`
*Bullet in the Design notes list.*

```
The two new audiences are separated by depth, not by a tab: everything a company needs comes first, candidates get the section below it.
```

#### details.design[4]
`src/data/landing-pages.ts → rise-talent.details.design[4]`
*Bullet in the Design notes list.*

```
Specificity over category. Twelve named job titles do more than five industry words.
```

#### details.build[0]
`src/data/landing-pages.ts → rise-talent.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 415 KB. Most of that is the client logos and photographs embedded directly in the file, so the page has no external image requests of its own to fail.
```

#### details.build[1]
`src/data/landing-pages.ts → rise-talent.details.build[1]`
*Bullet in the Build notes list.*

```
Hand-written CSS and JavaScript, no framework and no build step. Their current site runs on a page builder, which is where a lot of the weight on it comes from.
```

#### details.build[2]
`src/data/landing-pages.ts → rise-talent.details.build[2]`
*Bullet in the Build notes list.*

```
The role filters swap the visible tags on a data attribute and update aria-pressed, so the state is announced rather than only shown.
```

#### details.build[3]
`src/data/landing-pages.ts → rise-talent.details.build[3]`
*Bullet in the Build notes list.*

```
Eight figures count up on IntersectionObserver and hold their final value for anyone who prefers reduced motion.
```

#### details.build[4]
`src/data/landing-pages.ts → rise-talent.details.build[4]`
*Bullet in the Build notes list.*

```
Four breakpoints, 1040, 860, 640 and 520 pixels.
```

#### details.build[5]
`src/data/landing-pages.ts → rise-talent.details.build[5]`
*Bullet in the Build notes list.*

```
The annotation layer is part of the file: a Show notes button reveals a note on every section explaining what changed. It is off by default. Deployed on Netlify.
```

---

### Sunny Sky's Animal Rescue

**Page:** `/landing-page-sunny-skys`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Sunny Sky's Animal Rescue, a 501(c)(3) in Graham, Washington (unsolicited concept, not commissioned) · kind: Concept redesign · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Nonprofit design, Accessible accordions, Responsive layout, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → sunny-skys.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A redesign of the homepage for an animal rescue, built without being asked. Everything on it is theirs: their story, their photographs, their links, their tax ID. <strong>What changed is that adopting, volunteering and donating are all reachable from the first screen instead of partway down a long page.</strong>
```

#### description
`src/data/landing-pages.ts → sunny-skys.description`
*One or two sentences directly under the page title.*

```
A redesign of the Sunny Sky's Animal Rescue homepage, designed and built in 2026. It was not commissioned and I am not affiliated with them. Their story, photographs, links and details are theirs; the structure, the design and the code are mine. Press Show notes on the live page to see what changed in each section and why.
```

#### pageDescription
`src/data/landing-pages.ts → sunny-skys.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
An unsolicited concept redesign of the Sunny Sky's Animal Rescue homepage, shown section by section: a quick actions strip, their story, an adoption flow with clearly labeled sample listings, volunteering, donating and an FAQ.
```

#### facts[0]
`src/data/landing-pages.ts → sunny-skys.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site
```

#### heroCaption
`src/data/landing-pages.ts → sunny-skys.heroCaption`
*Caption under the first screenshot.*

```
The first screen. Their own photograph and their own tagline. The appointment-only rule sits directly under the buttons, because the most common wasted trip is somebody driving out expecting to walk in.
```

#### highlights[0]
`src/data/landing-pages.ts → sunny-skys.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Help in one tap — Adopt, volunteer, donate and call are all reachable from the first screen
```

#### highlights[1]
`src/data/landing-pages.ts → sunny-skys.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Nothing invented — Their story, photographs, links and tax ID, with every sample pet card labeled as a sample
```

#### highlights[2]
`src/data/landing-pages.ts → sunny-skys.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Scattered answers collected — The questions spread across their site and flyers put in one accordion
```

#### overviewTitle
`src/data/landing-pages.ts → sunny-skys.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page has to do
```

#### overview[0]
`src/data/landing-pages.ts → sunny-skys.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
A rescue homepage has four different visitors arriving at once: someone who wants to adopt, someone who wants to volunteer, someone who wants to give money, and someone checking whether they can visit today. On the existing site all four have to scroll the whole page to find out what they can do. So this version puts a strip of four actions immediately under the hero, and gives each one a real section further down.
```

#### overview[1]
`src/data/landing-pages.ts → sunny-skys.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The rest is about removing wasted trips and wasted emails. Visits are by appointment only and applications have to be approved first, so that rule appears in the hero, again at the top of the adoption steps, and again in the footer. The questions that are currently spread across their site and their flyers are collected into one accordion. Adoption fees are printed rather than requested.
```

#### overview[2]
`src/data/landing-pages.ts → sunny-skys.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
This was not commissioned and I am not affiliated with Sunny Sky's. Everything factual on the page is theirs: the 2021 fire and the sanctuary they are building in Graham, their PayPal and wishlist links, their drop-off locations, their tax ID and the children's book one of their adopters wrote. The adoptable pets are sample cards showing the layout, labeled as samples on every card, because their real animals live on Petfinder.
```

#### walkthroughIntro
`src/data/landing-pages.ts → sunny-skys.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/landing-pages.ts → sunny-skys.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → sunny-skys.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their photograph, their tagline, and the two things most visitors came to do. The line under the buttons is the one that saves everyone time: by appointment only, no walk-ins, and where the meetings happen. The strip below gives the other two audiences somewhere to go immediately.
```

#### sections[1].title
`src/data/landing-pages.ts → sunny-skys.sections[1].title`
*Short name of one section of the page being walked through.*

```
Our story
```

#### sections[1].caption
`src/data/landing-pages.ts → sunny-skys.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The 2021 fire, the years displaced, and the sanctuary they are building now. It was hard to find on the original site, so it moves to a section near the top with their own photograph and a link to the full version.
```

#### sections[2].title
`src/data/landing-pages.ts → sunny-skys.sections[2].title`
*Short name of one section of the page being walked through.*

```
Adopt
```

#### sections[2].caption
`src/data/landing-pages.ts → sunny-skys.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Their adoption process as three numbered steps, with the approved-first and no-walk-ins rule in step one where it cannot be missed. Adoption fees are printed rather than left to an email. The pet cards are filterable and every one carries a sample listing label, with a line underneath pointing to Petfinder for the real animals.
```

#### sections[3].title
`src/data/landing-pages.ts → sunny-skys.sections[3].title`
*Short name of one section of the page being walked through.*

```
Volunteer
```

#### sections[3].caption
`src/data/landing-pages.ts → sunny-skys.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A rescue this size runs on volunteers, so this is a real section rather than a line in the footer. The six areas are the specific ones they need help with, taken from their own volunteer material, and the age requirement and address are stated rather than hidden behind the application.
```

#### sections[4].title
`src/data/landing-pages.ts → sunny-skys.sections[4].title`
*Short name of one section of the page being walked through.*

```
Support the rescue
```

#### sections[4].caption
`src/data/landing-pages.ts → sunny-skys.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Every way of giving in one place: online through their PayPal, in person at their two drop-off points, or through their wishlists at three retailers. The 501(c)(3) line with the tax ID sits underneath, because donors going through an employer match need it and currently have to ask.
```

#### sections[5].title
`src/data/landing-pages.ts → sunny-skys.sections[5].title`
*Short name of one section of the page being walked through.*

```
The BLUE book
```

#### sections[5].caption
`src/data/landing-pages.ts → sunny-skys.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One of their adopters wrote a children's book and gives them a share of the proceeds, which is a genuinely unusual thing to be able to say. The cover is a stand-in that says so, because the artwork belongs to the author and was not mine to recreate.
```

#### sections[6].title
`src/data/landing-pages.ts → sunny-skys.sections[6].title`
*Short name of one section of the page being walked through.*

```
Good to know
```

#### sections[6].caption
`src/data/landing-pages.ts → sunny-skys.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Six questions in an accordion. Every answer already existed somewhere on their site or their flyers; the work was collecting them. Visiting, cost, location, applying, volunteering and whether donations are deductible.
```

#### sections[7].title
`src/data/landing-pages.ts → sunny-skys.sections[7].title`
*Short name of one section of the page being walked through.*

```
Events and footer
```

#### sections[7].caption
`src/data/landing-pages.ts → sunny-skys.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
They run adoption events and pop-ups through the year, and those live on their events page and Facebook, so a short banner points at both rather than duplicating a calendar that would go stale. The footer repeats the appointment rule one last time.
```

#### phonesIntro
`src/data/landing-pages.ts → sunny-skys.phonesIntro`
*One line introducing the phone screenshots.*

```
The page on a phone. Rescue traffic comes off Facebook, so most of it arrives here.
```

#### phones[0].caption
`src/data/landing-pages.ts → sunny-skys.phones[0].caption`
*Caption under one phone screenshot.*

```
The two buttons go full width and the appointment rule stays directly under them.
```

#### phones[1].caption
`src/data/landing-pages.ts → sunny-skys.phones[1].caption`
*Caption under one phone screenshot.*

```
The four actions stack into a tappable list at phone width.
```

#### phones[2].caption
`src/data/landing-pages.ts → sunny-skys.phones[2].caption`
*Caption under one phone screenshot.*

```
Pet cards go one per row, and the sample listing label stays on every photograph.
```

#### phones[3].caption
`src/data/landing-pages.ts → sunny-skys.phones[3].caption`
*Caption under one phone screenshot.*

```
Giving options stack with their buttons intact, so no one has to pinch to find PayPal.
```

#### details.design[0]
`src/data/landing-pages.ts → sunny-skys.details.design[0]`
*Bullet in the Design notes list.*

```
Their yellow and their logo stayed. The yellow is used for actions only, so every button on the page means the same thing.
```

#### details.design[1]
`src/data/landing-pages.ts → sunny-skys.details.design[1]`
*Bullet in the Design notes list.*

```
Their own photographs carry the page. A rescue has better pictures than any stock library and using anything else would be a mistake.
```

#### details.design[2]
`src/data/landing-pages.ts → sunny-skys.details.design[2]`
*Bullet in the Design notes list.*

```
Four actions immediately under the hero, because a rescue homepage has four different visitors arriving at once and making all of them scroll costs the rescue money.
```

#### details.design[3]
`src/data/landing-pages.ts → sunny-skys.details.design[3]`
*Bullet in the Design notes list.*

```
The appointment-only rule appears three times: in the hero, in the first adoption step and in the footer. Repetition here saves somebody a drive.
```

#### details.design[4]
`src/data/landing-pages.ts → sunny-skys.details.design[4]`
*Bullet in the Design notes list.*

```
Every sample pet card is labeled as a sample, and the book cover says the artwork was not reproduced. Nothing on the page pretends to be something it is not.
```

#### details.build[0]
`src/data/landing-pages.ts → sunny-skys.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 561 KB. Most of that is their photographs embedded directly in the file.
```

#### details.build[1]
`src/data/landing-pages.ts → sunny-skys.details.build[1]`
*Bullet in the Build notes list.*

```
Hand-written CSS and JavaScript, no framework and no build step.
```

#### details.build[2]
`src/data/landing-pages.ts → sunny-skys.details.build[2]`
*Bullet in the Build notes list.*

```
The pet cards filter on a data attribute, and the FAQ uses native details elements so it works before the JavaScript runs.
```

#### details.build[3]
`src/data/landing-pages.ts → sunny-skys.details.build[3]`
*Bullet in the Build notes list.*

```
Reveal on scroll and four counting figures run on IntersectionObserver, and both switch off for anyone who prefers reduced motion.
```

#### details.build[4]
`src/data/landing-pages.ts → sunny-skys.details.build[4]`
*Bullet in the Build notes list.*

```
Six breakpoints, from 1000 pixels down to 250, because rescue traffic includes a lot of old phones.
```

#### details.build[5]
`src/data/landing-pages.ts → sunny-skys.details.build[5]`
*Bullet in the Build notes list.*

```
The annotation layer is part of the file: a Show notes button reveals a note on every section explaining what changed. It is off by default. Deployed on Netlify.
```

---

### Almanac Coffee Bar

**Page:** `/landing-page-almanac`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Almanac Coffee Bar (an invented business) · kind: Concept design · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Accessible tabs and dialogs, Responsive layout, SVG, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → almanac.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A one-page site for a neighborhood coffee bar. It answers the two questions people actually have, what is on the menu and when are you open, without making anyone search for them. <strong>The menu filters by category, the plus buttons build an order in a drawer, and the events book through a dialog.</strong> One hand-written HTML file.
```

#### description
`src/data/landing-pages.ts → almanac.description`
*One or two sentences directly under the page title.*

```
A one-page site for a neighborhood coffee bar, designed and built in 2026. Almanac is not a real business. I invented it so I could design and build every part of a small hospitality site without waiting for a client to ask for one.
```

#### pageDescription
`src/data/landing-pages.ts → almanac.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
A one-page concept site for a neighborhood coffee bar, shown section by section: a filterable menu with an order-ahead drawer, a coffee of the month, bookable events, a photo wall, reviews and a visit section with hours and a map.
```

#### facts[0]
`src/data/landing-pages.ts → almanac.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site
```

#### heroCaption
`src/data/landing-pages.ts → almanac.heroCaption`
*Caption under the first screenshot.*

```
The first screen. The photograph is the room, the card on the right is what came out of the oven this morning, and the rating sits directly under the buttons.
```

#### highlights[0]
`src/data/landing-pages.ts → almanac.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Order ahead — The plus button on any menu row opens a drawer and builds the order without leaving the page
```

#### highlights[1]
`src/data/landing-pages.ts → almanac.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Bookable events — Each event shows what is left and books through a dialog
```

#### highlights[2]
`src/data/landing-pages.ts → almanac.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One HTML file — About 94 KB, hand-written, no framework and no build step
```

#### overviewTitle
`src/data/landing-pages.ts → almanac.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page has to do
```

#### overview[0]
`src/data/landing-pages.ts → almanac.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
A coffee bar gets two kinds of visitor: someone deciding whether to walk in, and a regular checking the hours or what is on today. Both questions have to be answered without hunting, so the menu with prices, the hours and the address are all on the one page, and an order-ahead button rides in the header the whole way down.
```

#### overview[1]
`src/data/landing-pages.ts → almanac.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The order of the sections follows how someone decides. The menu first, because that is what people came for, then the coffee of the month and where the beans come from, then the things that make it a place rather than a counter: the events, the photo wall, the reviews and the three people behind it. The page ends with the practical part, hours and address and a map, next to an email club for people who want to hear when the roast changes.
```

#### overview[2]
`src/data/landing-pages.ts → almanac.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Almanac is not a real business. I invented it so every detail would be mine to write: the drinks, the prices, the roast notes, the events and the staff are all written out, not filled in with placeholder text. The point was to build a small hospitality site the way one should be built, and have something to show for it.
```

#### walkthroughIntro
`src/data/landing-pages.ts → almanac.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/landing-pages.ts → almanac.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → almanac.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A photograph of the bar being worked, one line saying what the place is, and two buttons. The card on the right is today's bake, which turns the page from a brochure into something worth checking again. The Google rating sits under the buttons rather than in a badge nobody reads.
```

#### sections[1].title
`src/data/landing-pages.ts → almanac.sections[1].title`
*Short name of one section of the page being walked through.*

```
The menu
```

#### sections[1].caption
`src/data/landing-pages.ts → almanac.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The menu, with prices, on the page. Four tabs switch between espresso, brewed, not coffee and food without a reload, and they are built as a real tab list, so the arrow keys move between them. The plus button on any row adds it to an order drawer that totals at the bottom and says you pay at the counter.
```

#### sections[2].title
`src/data/landing-pages.ts → almanac.sections[2].title`
*Short name of one section of the page being walked through.*

```
Coffee of the month
```

#### sections[2].caption
`src/data/landing-pages.ts → almanac.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
One roast given a whole section: where it grew, what it tastes like, and four numbered steps for brewing it at home. It ends with both prices, a cup and a bag, because the people who read this far are the ones who buy beans.
```

#### sections[3].title
`src/data/landing-pages.ts → almanac.sections[3].title`
*Short name of one section of the page being walked through.*

```
Our coffee
```

#### sections[3].caption
`src/data/landing-pages.ts → almanac.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Where the beans come from and how they are roasted, in plain sentences. Three figures sit under it, and the three stages from green bean to cup are named, so the sourcing claim is specific rather than general.
```

#### sections[4].title
`src/data/landing-pages.ts → almanac.sections[4].title`
*Short name of one section of the page being walked through.*

```
This week at Almanac
```

#### sections[4].caption
`src/data/landing-pages.ts → almanac.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three events this week, each with the day, the time, the price and how many spots are left. Save a spot opens a dialog. The spots-left count gives a reason to book now rather than later.
```

#### sections[5].title
`src/data/landing-pages.ts → almanac.sections[5].title`
*Short name of one section of the page being walked through.*

```
The vibe
```

#### sections[5].caption
`src/data/landing-pages.ts → almanac.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
A photo wall of the room, the food and the people in it, in a grid of mixed sizes so it does not read as a stock gallery. Any photo opens full size in a viewer with arrow keys and an Escape to close.
```

#### sections[6].title
`src/data/landing-pages.ts → almanac.sections[6].title`
*Short name of one section of the page being walked through.*

```
Kind words
```

#### sections[6].caption
`src/data/landing-pages.ts → almanac.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The rating broken down star by star, next to the reviews themselves in a slider. The breakdown is more useful than the average on its own: 91 percent of the reviews are five stars.
```

#### sections[7].title
`src/data/landing-pages.ts → almanac.sections[7].title`
*Short name of one section of the page being walked through.*

```
The people
```

#### sections[7].caption
`src/data/landing-pages.ts → almanac.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The three people behind the counter, each with what they do and one specific thing about them. A small independent place competes on exactly this, so it gets a section rather than a line in the footer.
```

#### sections[8].title
`src/data/landing-pages.ts → almanac.sections[8].title`
*Short name of one section of the page being walked through.*

```
Good to know
```

#### sections[8].caption
`src/data/landing-pages.ts → almanac.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Six questions in an accordion: parking, working on a laptop, dogs, catering, gift cards and allergens. These are the ones that otherwise arrive as Instagram messages one at a time.
```

#### sections[9].title
`src/data/landing-pages.ts → almanac.sections[9].title`
*Short name of one section of the page being walked through.*

```
Visit us and footer
```

#### sections[9].caption
`src/data/landing-pages.ts → almanac.sections[9].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Hours with today picked out and an open or closed line above them, the address, a phone number, directions and a map drawn as inline SVG rather than loaded from a map service. The email club sits beside it, and the footer repeats the practical details for anyone who scrolled past.
```

#### phonesIntro
`src/data/landing-pages.ts → almanac.phonesIntro`
*One line introducing the phone screenshots.*

```
The page on a phone, which is where most people will open it, standing outside or on the way over.
```

#### phones[0].caption
`src/data/landing-pages.ts → almanac.phones[0].caption`
*Caption under one phone screenshot.*

```
The hero keeps the photograph, the two buttons and the bake of the day above the fold.
```

#### phones[1].caption
`src/data/landing-pages.ts → almanac.phones[1].caption`
*Caption under one phone screenshot.*

```
The menu tabs wrap to two lines and the prices stay full width, so ordering works one-handed.
```

#### phones[2].caption
`src/data/landing-pages.ts → almanac.phones[2].caption`
*Caption under one phone screenshot.*

```
The three event cards become a strip you swipe, rather than three tall cards to scroll past.
```

#### phones[3].caption
`src/data/landing-pages.ts → almanac.phones[3].caption`
*Caption under one phone screenshot.*

```
Open or closed first, then hours, then directions and a call button within thumb reach.
```

#### details.design[0]
`src/data/landing-pages.ts → almanac.details.design[0]`
*Bullet in the Design notes list.*

```
Two typefaces: Lora for headlines, Figtree for everything else. The palette is cream, dark roast brown and amber, taken from the product rather than from a template.
```

#### details.design[1]
`src/data/landing-pages.ts → almanac.details.design[1]`
*Bullet in the Design notes list.*

```
Prices are on the page. Nothing is hidden behind a PDF, a photo of a chalkboard or a delivery app.
```

#### details.design[2]
`src/data/landing-pages.ts → almanac.details.design[2]`
*Bullet in the Design notes list.*

```
Photographs carry the food and the room. Everything else is type, space and one accent color.
```

#### details.design[3]
`src/data/landing-pages.ts → almanac.details.design[3]`
*Bullet in the Design notes list.*

```
Every section that could end in an order ends in the same amber button, and the header carries it the whole way down.
```

#### details.design[4]
`src/data/landing-pages.ts → almanac.details.design[4]`
*Bullet in the Design notes list.*

```
The details are written, not filled in: real drink names, real roast notes, named staff and specific events.
```

#### details.build[0]
`src/data/landing-pages.ts → almanac.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 94 KB, with hand-written CSS and JavaScript. No framework and no build step.
```

#### details.build[1]
`src/data/landing-pages.ts → almanac.details.build[1]`
*Bullet in the Build notes list.*

```
The menu is a real ARIA tab list with arrow-key navigation, and each of the 25 add buttons carries its own label for screen readers.
```

#### details.build[2]
`src/data/landing-pages.ts → almanac.details.build[2]`
*Bullet in the Build notes list.*

```
Order ahead opens a modal drawer, the photo wall and the event booking use the native dialog element, so all three close on Escape and trap focus without a library.
```

#### details.build[3]
`src/data/landing-pages.ts → almanac.details.build[3]`
*Bullet in the Build notes list.*

```
Reveal on scroll and the counting numbers run on IntersectionObserver, and both switch off for anyone who prefers reduced motion.
```

#### details.build[4]
`src/data/landing-pages.ts → almanac.details.build[4]`
*Bullet in the Build notes list.*

```
Breakpoints run down to 340 pixels wide. The events row turns into a horizontal strip on a phone rather than three stacked cards.
```

#### details.build[5]
`src/data/landing-pages.ts → almanac.details.build[5]`
*Bullet in the Build notes list.*

```
Every icon and the map are inline SVG. The photographs are the only image files. Deployed on Netlify.
```

---

### Wasatch Ridge Capital

**Page:** `/landing-page-wasatch-ridge`  
**Fixed labels (not prose, do not rewrite):** eyebrow: Landing page · client: Wasatch Ridge Capital (an invented firm) · kind: Concept design · role: Design and front-end development · year: 2026  
**Tags:** HTML & CSS, JavaScript, Data presentation, Long-form layout, SVG, Netlify  
**Tools:** HTML & CSS, JavaScript, SVG, Google Fonts, Netlify  

#### summary
`src/data/landing-pages.ts → wasatch-ridge.summary`
*Card text in the homepage grid. This is the first thing a visitor reads about this page. Inline <strong> allowed.*

```
A one-page concept site for a real estate investment firm. <strong>Wasatch Ridge is an invented firm, every figure is labeled illustrative, and a concept notice appears in four places.</strong> It includes a filterable portfolio, a distributions chart, three offerings and an accredited-investor form, hand-written in a single HTML file.
```

#### description
`src/data/landing-pages.ts → wasatch-ridge.description`
*One or two sentences directly under the page title.*

```
A one-page site for a real estate investment firm, designed and built in 2026. Wasatch Ridge is an invented firm and none of the figures are real. I built it to practice presenting dense financial data clearly and to handle the disclosure a page like this needs.
```

#### pageDescription
`src/data/landing-pages.ts → wasatch-ridge.pageDescription`
*Meta description. Shown in search results and link previews, never on the page itself. Aim for 150 to 160 characters.*

```
A one-page concept site for a real estate investment firm, shown section by section: a filterable portfolio, a track record with a distributions chart, three offerings, an FAQ and an accredited-investor access form.
```

#### facts[0]
`src/data/landing-pages.ts → wasatch-ridge.facts[0]`
*Label in the facts strip near the top of the page.*

```
Format: One-page site
```

#### heroCaption
`src/data/landing-pages.ts → wasatch-ridge.heroCaption`
*Caption under the first screenshot.*

```
The first screen. The illustration is SVG rather than a photograph, the accredited-investor notice sits above the fold, and four key figures close the section.
```

#### highlights[0]
`src/data/landing-pages.ts → wasatch-ridge.highlights[0]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Labeled throughout — Every figure, property and offering is marked illustrative, with a concept notice in four places
```

#### highlights[1]
`src/data/landing-pages.ts → wasatch-ridge.highlights[1]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
Filterable portfolio — Six properties by type, each with units, year acquired and occupancy
```

#### highlights[2]
`src/data/landing-pages.ts → wasatch-ridge.highlights[2]`
*Stat tile under the first screen. Format is: large value, then an explaining line.*

```
One HTML file — About 52 KB, hand-written, no framework and no build step
```

#### overviewTitle
`src/data/landing-pages.ts → wasatch-ridge.overviewTitle`
*Heading above the overview paragraphs.*

```
What the page does
```

#### overview[0]
`src/data/landing-pages.ts → wasatch-ridge.overview[0]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
The page answers four questions in order: what the firm buys, whether it has done it before, what is open now, and what happens after you submit your details. The request form sits at the end, after all four are answered.
```

#### overview[1]
`src/data/landing-pages.ts → wasatch-ridge.overview[1]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Disclosure was the main constraint. Every figure on the page is invented, so the page states that clearly and repeatedly: a concept notice under the portfolio, under the offerings, in the footer and on the form. The chart caption marks the figures illustrative, and the returns figure carries an asterisk that resolves in the footer. Nothing on the page can be read as a solicitation.
```

#### overview[2]
`src/data/landing-pages.ts → wasatch-ridge.overview[2]`
*Overview paragraphs. The main prose: what the page had to do and how it was approached.*

```
Wasatch Ridge is not a real firm. I invented it for the constraints: dense figures that still have to scan on a phone, a portfolio filter built without a framework, and a form serious enough for the amount it asks about.
```

#### walkthroughIntro
`src/data/landing-pages.ts → wasatch-ridge.walkthroughIntro`
*One line introducing the section-by-section walkthrough below it.*

```
The page, top to bottom.
```

#### sections[0].title
`src/data/landing-pages.ts → wasatch-ridge.sections[0].title`
*Short name of one section of the page being walked through.*

```
Header and hero
```

#### sections[0].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[0].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The headline states what the firm does and where it operates. The illustration is drawn in SVG, so it loads instantly and avoids the stock skyline photograph. The accredited-investor notice sits above the fold rather than in the footer, and four figures close the section.
```

#### sections[1].title
`src/data/landing-pages.ts → wasatch-ridge.sections[1].title`
*Short name of one section of the page being walked through.*

```
Strategy
```

#### sections[1].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[1].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three strategies, each with a one-line rationale and a bar showing its share of the portfolio: 58 percent multifamily, 27 percent commercial and industrial, 15 percent value-add. The bars let a reader check the allocation instead of taking it on trust.
```

#### sections[2].title
`src/data/landing-pages.ts → wasatch-ridge.sections[2].title`
*Short name of one section of the page being walked through.*

```
Portfolio
```

#### sections[2].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[2].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Six properties, filterable by type without a page reload. Each card carries the numbers that matter for that asset class: units or square feet, the year acquired, and occupancy or lease term. The concept notice sits directly under the grid.
```

#### sections[3].title
`src/data/landing-pages.ts → wasatch-ridge.sections[3].title`
*Short name of one section of the page being walked through.*

```
Track record
```

#### sections[3].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[3].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The only section in the firm's dark green, which marks it as the center of the page. Three headline figures, then distributions by year as a bar chart built in CSS rather than an image, so it stays sharp at any size. The caption marks the figures illustrative and the returns figure carries an asterisk.
```

#### sections[4].title
`src/data/landing-pages.ts → wasatch-ridge.sections[4].title`
*Short name of one section of the page being walked through.*

```
How investing works
```

#### sections[4].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[4].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Four numbered steps from request to distribution, in plain language. It answers what happens after a visitor submits their details, which the form itself does not explain.
```

#### sections[5].title
`src/data/landing-pages.ts → wasatch-ridge.sections[5].title`
*Short name of one section of the page being walked through.*

```
Offerings
```

#### sections[5].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[5].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Three offerings with status on each card: open now, opening next quarter, and fully subscribed. Each shows target return, hold period, minimum investment and the share of the raise committed. The fully subscribed offering stays on the page as evidence of completed deals.
```

#### sections[6].title
`src/data/landing-pages.ts → wasatch-ridge.sections[6].title`
*Short name of one section of the page being walked through.*

```
Insights
```

#### sections[6].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[6].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Four articles written for investors rather than for search, each tagged by the part of the business it covers.
```

#### sections[7].title
`src/data/landing-pages.ts → wasatch-ridge.sections[7].title`
*Short name of one section of the page being walked through.*

```
Before you request access
```

#### sections[7].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[7].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
Six questions in an accordion, including the difficult ones: how long capital is committed, whether the firm invests alongside its investors, and what the risks are.
```

#### sections[8].title
`src/data/landing-pages.ts → wasatch-ridge.sections[8].title`
*Short name of one section of the page being walked through.*

```
Investor access and footer
```

#### sections[8].caption
`src/data/landing-pages.ts → wasatch-ridge.sections[8].caption`
*One to three sentences under a section screenshot: what that section does and how it was built.*

```
The request form: name, email, phone, investment amount as a range rather than a figure, a free-text field and an accredited-investor confirmation. Three reassurances sit beside it, and the footer carries the full disclaimer.
```

#### phonesIntro
`src/data/landing-pages.ts → wasatch-ridge.phonesIntro`
*One line introducing the phone screenshots.*

```
The page on a phone. The figures are the hardest part to keep readable at this width, so they were designed first.
```

#### phones[0].caption
`src/data/landing-pages.ts → wasatch-ridge.phones[0].caption`
*Caption under one phone screenshot.*

```
The illustration moves above the headline and the single button fills the width.
```

#### phones[1].caption
`src/data/landing-pages.ts → wasatch-ridge.phones[1].caption`
*Caption under one phone screenshot.*

```
Property cards stack one per screen, and the three figures stay in a row rather than wrapping.
```

#### phones[2].caption
`src/data/landing-pages.ts → wasatch-ridge.phones[2].caption`
*Caption under one phone screenshot.*

```
Each offering becomes a full-width card with its status badge and the committed bar intact.
```

#### phones[3].caption
`src/data/landing-pages.ts → wasatch-ridge.phones[3].caption`
*Caption under one phone screenshot.*

```
The three reassurances move above the form rather than beside it.
```

#### details.design[0]
`src/data/landing-pages.ts → wasatch-ridge.details.design[0]`
*Bullet in the Design notes list.*

```
Two typefaces: Lora for headlines, Instrument Sans for text and every figure. Numbers get the sans so columns of them line up.
```

#### details.design[1]
`src/data/landing-pages.ts → wasatch-ridge.details.design[1]`
*Bullet in the Design notes list.*

```
Dark green, cream and muted gold. The green appears once, on the track record, which marks it as the center of the page.
```

#### details.design[2]
`src/data/landing-pages.ts → wasatch-ridge.details.design[2]`
*Bullet in the Design notes list.*

```
The hero illustration is drawn rather than photographed, which avoids the stock skyline photograph most firms in this sector use.
```

#### details.design[3]
`src/data/landing-pages.ts → wasatch-ridge.details.design[3]`
*Bullet in the Design notes list.*

```
Figures are grouped in rows of three or four and never mixed with body copy, so a reader can scan the numbers without reading the page.
```

#### details.design[4]
`src/data/landing-pages.ts → wasatch-ridge.details.design[4]`
*Bullet in the Design notes list.*

```
Disclosure is built into the layout: the concept notice appears in four places, and the chart, the offerings and the returns figure each carry their own label.
```

#### details.build[0]
`src/data/landing-pages.ts → wasatch-ridge.details.build[0]`
*Bullet in the Build notes list.*

```
One HTML file, about 52 KB, with hand-written CSS and JavaScript. No framework and no build step.
```

#### details.build[1]
`src/data/landing-pages.ts → wasatch-ridge.details.build[1]`
*Bullet in the Build notes list.*

```
The portfolio filters on a data attribute and updates aria-pressed on the buttons, so the filter state is exposed to screen readers, not just shown visually.
```

#### details.build[2]
`src/data/landing-pages.ts → wasatch-ridge.details.build[2]`
*Bullet in the Build notes list.*

```
The distributions chart is CSS, with each bar height set by a custom property. It stays sharp at any size and is hidden from screen readers, which get the same figures as text above it.
```

#### details.build[3]
`src/data/landing-pages.ts → wasatch-ridge.details.build[3]`
*Bullet in the Build notes list.*

```
Ten numbers count up when their block comes into view, on IntersectionObserver, and hold their final value for anyone who prefers reduced motion.
```

#### details.build[4]
`src/data/landing-pages.ts → wasatch-ridge.details.build[4]`
*Bullet in the Build notes list.*

```
The FAQ uses native details elements, so it works before the JavaScript runs.
```

#### details.build[5]
`src/data/landing-pages.ts → wasatch-ridge.details.build[5]`
*Bullet in the Build notes list.*

```
Three breakpoints, 980, 680 and 480 pixels. Deployed on Netlify.
```

---
